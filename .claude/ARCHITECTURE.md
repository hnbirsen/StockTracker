# Mimari Dokümanı

## Genel Yaklaşım

Sistem, **database-per-service** prensibiyle çalışan bağımsız mikroservislerden oluşur. Dış client trafiği YARP Gateway üzerinden gelir. Servisler arası iç iletişim ise doğrudan HTTP ile yapılır — gateway'den geçmez.

## Servisler

| Servis | Sorumluluk | Veritabanı | Port | Durum |
|---|---|---|---|---|
| Identity Service | Kayıt, login, JWT/refresh token, logout | `identity_db` | 5001 | ✅ Tamamlandı |
| Product Service | Barkod/kod lookup, marka eşleştirme, Redis cache | `product_db` | 5002 | ✅ Tamamlandı |
| Brand Detection Service | Regex format eşleşmesi, manuel marka seçimi | `brand_db` | 5003 | ✅ Tamamlandı |
| Store Reference Service | İl/ilçe → marka-spesifik mağaza ID eşleştirme | `store_db` | 5004 | ✅ Tamamlandı |
| Search Orchestrator | Kullanıcı sorgusunu kuyruğa yönlendirme | — (Redis: throttle) | 5005 | ✅ Tamamlandı |
| Subscription Service | Watch group, takip listesi, dedup | `subscription_db` | 5006 | ✅ Faz 3.1 tamamlandı |
| Billing Service | Freemium plan yönetimi, App Store/Play Store IAP doğrulama + webhook | `billing_db` | 5007 | ✅ Faz 4.1 + 4.2 tamamlandı (gerçek Apple/Google hesabı hariç) |
| Notification Service | Push (FCM) + e-posta bildirimleri | `notification_db` | 5008 | ✅ Faz 3.3 tamamlandı (gerçek Firebase/SMTP sunucusu hariç) |
| Bershka Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5009 | ✅ Gerçek API entegre edildi (bkz. altta) |
| Zara Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5010 | ✅ Gerçek API entegre edildi (bkz. altta) |
| Mango Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5011 | ✅ Gerçek API entegre edildi, uçtan uca canlı doğrulandı (bkz. altta) |
| H&M Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5012 | ⚠️ Kod tamamlandı, unit testler geçiyor — canlı Chrome/Playwright doğrulaması Akamai otomasyon-tespiti nedeniyle şu an yapılamıyor (bkz. altta) |
| Massimo Dutti Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5013 | ✅ Kod + 21 unit test tamamlandı, mağaza sorgusu gerçek sayısal stokla canlı doğrulandı (bkz. altta) |
| Beymen Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: ürün özeti cache-aside, kendi DB'si yok) | 5014 | ✅ Kod + 18 unit test tamamlandı, Playwright hiç yok, gerçek API'lerle canlı doğrulandı (bkz. altta) |
| Pull&Bear Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP cache-aside, kendi DB'si yok) | 5015 | ✅ Kod + 20 unit test tamamlandı, mağaza sorgusu gerçek sayısal stokla canlı doğrulandı (bkz. altta) |
| Stradivarius Scraper | `CheckStockCommand` tüketir, `StockResultEvent` yayınlar | — (Redis: PDP+token cache-aside, kendi DB'si yok) | 5016 | ✅ Kod + 20 unit test tamamlandı, mağaza sorgusu gerçek sayısal stokla canlı doğrulandı (bkz. altta) |
| API Gateway (YARP) | Dış trafik yönlendirme, JWT doğrulama, rate limiting | — | 8000 | ✅ Tamamlandı |

## Servisler Arası İletişim

### Dış Trafik (Client → Servis)
Client'lardan (web/mobil) gelen tüm istekler API Gateway (port 8000) üzerinden geçer. Gateway JWT doğrulaması ve rate limiting yapar, ardından ilgili servise yönlendirir.

### İç Trafik (Servis → Servis)
Servisler birbirleriyle **doğrudan HTTP** üzerinden iletişim kurar — gateway bypass edilir. Bu yaklaşımın nedenleri:
- Gereksiz gecikme ve hop'tan kaçınmak
- Gateway down olduğunda iç iletişimin etkilenmemesi
- İç endpoint'lerin dışarıya açılmaması

Mevcut iç iletişim: `BrandDetection → Product Service` (mapping kaydetmek için)

### Asenkron (RabbitMQ)
Stok değişikliği, bildirim tetikleme, scraping tamamlanma gibi olaylar event olarak yayınlanır:
1. Scraper stok değişikliğini tespit eder → `StockResultEvent` yayınlar
2. Notification Service event'i dinler → kullanıcıya push/e-posta gönderir

## YARP Gateway Route Yapısı

```
/api/identity/*        → Identity Service       (5001)
/api/product/*         → Product Service         (5002)
/api/brand-detection/* → Brand Detection Service (5003)
/api/store-reference/* → Store Reference Service (5004)
/api/search/*          → Search Orchestrator     (5005)
/api/subscriptions/*   → Subscription Service    (5006)
/api/billing/*         → Billing Service         (5007)
/api/notifications/*   → Notification Service    (5008)
```

> `PathPattern` ve `PathRemovePrefix` transform'ları **aynı blokta kullanılamaz** — her biri ayrı transform bloğunda tanımlanır.

## Kimlik Doğrulama Akışı

1. Client → `POST /api/identity/auth/login` → Identity Service JWT üretir
2. Client her istekte `Authorization: Bearer <token>` header'ı gönderir
3. Gateway JWT'yi doğrular, geçerliyse servise iletir
4. Access token süresi dolunca → `POST /api/identity/auth/refresh` ile yeni token çifti alınır

**İç iletişim (Faz 3.3)**: `GET /internal/users/{id}` — gateway bypass, direkt HTTP. Notification Service, bildirim göndereceği kullanıcının email adresini buradan çözer.

## Marka Tespit Akışı (Brand Detection)

```
Kullanıcı ürün kodu girer
    ↓
Product Service — ProductBrandMap'te var mı? (Redis cache → DB)
    ↓ Hayır
Brand Detection Service — format imzası eşleşmesi (regex)
    ↓ Tek + yüksek güvenilirlik → Product Service'e otomatik kaydet
    ↓ Çoklu aday veya düşük güvenilirlik → kullanıcıya aday listesi sun
    ↓ Kullanıcı seçti → POST /resolve/manual → Product Service'e kaydet
```

Katmanlar (öncelik sırasıyla):
1. **Regex format signature** — bilinen marka kod formatlarıyla eşleştirme (şu an aktif)
2. **Site-search API fallback** — markanın site içi arama API'si (Faz 2'de eklenecek)
3. **Genel arama motoru fallback** — yukarıdakiler başarısız olursa (Faz 2'de eklenecek)

Başarılı eşleşmeler `ProductBrandMap` tablosuna yazılır → sonraki sorgular Redis cache'ten anında döner.

## Scraping Mimarisi (Faz 2)

- Her marka için ayrı, izole scraper worker servisi
- RabbitMQ üzerinden `CheckStockCommand` alır, `StockResultEvent` yayınlar
- Polly ile retry + circuit breaker — bir markanın engellenmesi diğerlerini etkilemez
- Scraper health monitoring — başarı oranı düşünce alert üretir

### Mesajlaşma Katmanı (Faz 2.1 — Tamamlandı)

- **Sözleşmeler**: `StockTracker.Shared.Contracts/Messages/V1/` altında `CheckStockCommand`, `StockResultEvent` ve (Faz 4.1) `UserRegisteredEvent` — namespace bazlı versiyonlama (`Messages.V1`). Breaking değişiklik gerektiğinde eski consumer'ları bozmadan `Messages.V2` namespace'i eklenir.
- **Kuyruk isimlendirme**: `QueueNaming.StockCheckQueue(brandName)` → `stock.check.{brandName}` (küçük harf). Her markanın kendi izole kuyruğu vardır; bir markanın scraper'ı tıkandığında diğerleri etkilenmez.
- **`CheckStockCommand`** marka-spesifik kuyruğa doğrudan **send** edilir (point-to-point — komutu sadece o markanın scraper'ı işler). **`StockResultEvent`** ise **publish** edilir (fanout — sonuca birden fazla dinleyici, örn. Notification Service ve Search Orchestrator, ilgi duyabilir).
- **MassTransit vs ham RabbitMQ.Client kararı**: MassTransit seçildi — Polly'yle tutarlı retry/circuit-breaker middleware'i, connection/topology yönetimini soyutlaması, ve test edilebilirliği (`MassTransit.TestFramework`) nedeniyle. ⚠️ **MassTransit v9+ ticari lisans gerektiriyor** (bkz. `SetLicense`/`MT_LICENSE`); proje son açık kaynak (Apache 2.0) sürüm olan **8.5.5**'e sabitlendi (`StockTracker.Shared.Contracts.csproj`). Paket sürümü ileride yükseltilecekse önce lisans gereksinimi kontrol edilmeli.
- **Kurulum**: `StockTracker.Shared.Contracts.Messaging.ServiceCollectionExtensions.AddStockTrackerRabbitMq(...)` — tüm servislerin ortak kullandığı bağlantı/host okuma mantığı (`RABBITMQ_HOST`/`RABBITMQ_USER`/`RABBITMQ_PASSWORD`). Consumer kaydı ve endpoint routing'i `configureConsumers`/`configureEndpoints` delegate'leri ile servis bazlı yapılır.
- **Uçtan uca doğrulama**: Gerçek RabbitMQ container'ına karşı `CheckStockCommand` → `stock.check.bershka` kuyruğuna gönderim → consume → `StockResultEvent` publish → consume round-trip'i manuel bir smoke-test ile doğrulandı (kod kalıcı değil, sadece doğrulama amaçlıydı). Search Orchestrator (Faz 2.2) ve Bershka Scraper (Faz 2.4) servisleri gerçek iş mantığıyla bu topolojiyi kullanacak.

### Search Orchestrator (Faz 2.2 — Tamamlandı)

- `POST /search` — `{ userId, productCode, size, locations? }` alır. Akış: Product Service'e `GET /lookup/{code}` → çözülmemişse Brand Detection'a `POST /resolve` → tek+yüksek-güven otomatik kaydedilmişse Product tekrar sorgulanır. Marka biliniyorsa her lokasyon için (veya lokasyon yoksa tek "online" komut için) `CheckStockCommand` `stock.check.{ScraperQueueName}` kuyruğuna **send** edilir (point-to-point, `ISendEndpointProvider.GetSendEndpoint(new Uri("queue:..."))`). `StoreId` şu an her zaman `null` — gerçek mağaza eşlemesi Faz 2.3'te Store Reference Service ile gelecek; City/District ham metin olarak taşınır.
- **Throttle**: Redis `search:throttle:{userId}:{productCode}:{size}` key'i `SETNX` ile 30 saniyelik pencerede kilitlenir — aynı kullanıcı aynı ürün/beden için art arda istek atarsa `429 Too Many Requests` döner.
- **Yanıt tasarımı**: Marka biliniyorsa `202 Accepted` + `{searchId, status:"Queued", message:"..."}` (asenkron, sonuç ileride bildirimle gelecek). Marka bilinmiyorsa (aday yok veya çoklu aday/manuel seçim gerekiyor) `200 OK` + `status:"BrandUnknown"` + varsa aday listesi — client bunu doğrudan `/api/brand-detection/resolve/manual`'a yönlendirebilir.
- Servis stateless'tir (kendi veritabanı yok); Product/Brand Detection/Store Reference ile internal HTTP (`ProductServiceClient`, `BrandDetectionServiceClient`, `StoreReferenceServiceClient`), RabbitMQ ile mesajlaşma, Redis ile throttle durumu tutar.

### Store Reference Service (Faz 2.3 — Tamamlandı)

- `GET /stores?brandId=&city=&district=` — üçü de opsiyonel filtre; sadece `IsActive=true` mağazalar döner. `city`/`district` karşılaştırması case-insensitive.
- `Stores` tablosu: `BrandId`/`BrandName` (product_db.Brands ile convention-based referans, FK değil), `City`, `District`, `StoreName`, `BrandSpecificStoreId` (markanın kendi sitesindeki mağaza kodu — scraper bunu kullanacak).
- Seed data: Bershka için 4 mağaza (Istanbul/Kadıköy, Istanbul/Şişli, Ankara/Çankaya, Izmir/Bornova). `BrandSpecificStoreId` değerleri şu an yer tutucudur (`BSK-IST-KDK-01` gibi) — Faz 2.4'te Bershka scraper'ı geliştirilirken gerçek mağaza kodlarıyla güncellenmesi gerekiyor.
- **Search Orchestrator entegrasyonu**: bir arama isteğinde konum (city/district) verildiğinde, Search Orchestrator önce `GET /stores?brandId=&city=&district=` ile bu markanın o ilçedeki mağazalarını sorgular. Eşleşme varsa her mağaza için ayrı `CheckStockCommand` (`StoreId` dolu) gönderilir; eşleşme yoksa `StoreId=null` ile fallback komut gönderilir (scraper en azından online stok kontrolü yapabilsin diye — bkz. Mesajlaşma Katmanı notu).
- **`GET /stores/{id}`** (Faz 3.2'de eklendi): tek bir mağazayı ID'sinden döner (`IsActive=false` ise 404). Subscription Service'in Stock Poller'ı, `WatchGroup.StoreId`'den `BrandSpecificStoreId`'yi çözmek için bunu kullanır — `GET /stores` yalnızca liste/filtre destekliyordu, poller'ın ihtiyaç duyduğu tekil ID sorgusu yoktu.

### Bershka Scraper (Faz 2.4 — Tamamlandı)

- **Consumer**: `CheckStockCommandConsumer`, `stock.check.bershka` kuyruğunu dinler. `CheckStockCommand` (**V2**, bkz. altta) alır, `BershkaStockCheckService.CheckAsync(...)` çağırır, dönen `StockResultEvent`'i (V1, değişmedi) publish eder.
- **Yönlendirme mantığı** (`BershkaStockCheckService`): önce `command.ProductUrl` boş mu diye bakılır — boşsa API client'a hiç gidilmeden `Unknown` (`ScraperSource: "no-product-url"`) dönülür. Doluysa: `command.StoreId` doluysa VE `BrandSpecificStoreId` de doluysa → `IBershkaStockApiClient.CheckStoreStockAsync(...)` (fiziksel mağaza kontrolü); aksi halde → `CheckOnlineStockAsync(...)` (online kontrol).
- **Dayanıklılık**: Stok API HttpClient'ı `AddTransientHttpErrorPolicy` ile 3 kez exponential backoff retry (2s, 4s, 8s) + art arda 5 hatada 30 saniyelik devre kesici kullanır (`Microsoft.Extensions.Http.Polly`). `ScraperEtiquetteHandler` her istekte 4 gerçekçi tarayıcı User-Agent'ı arasından rastgele seçim yapar ve 300-1200ms rastgele gecikme uygular (bkz. `.claude/SECURITY.md`). Prodüksiyona karşı doğrulandı: varsayılan `curl` User-Agent'ı ile istek **403** dönerken, gerçekçi bir tarayıcı User-Agent'ı ile aynı istek **200** dönüyor.

#### `CheckStockCommand` V2 — neden `ProductUrl` eklendi

`Messages.V1.CheckStockCommand` değişmez (dokümante edilmiş kural). `Messages.V2.CheckStockCommand` tek bir alan ekliyor: `ProductUrl` (Product Service'in `ProductBrandMap.ProductUrl` alanından geliyor, sadece manuel/site-search ile çözülmüş mapping'lerde dolu — regex-only çözümlerde null). Search Orchestrator artık V2 gönderiyor, BershkaScraper V2 tüketiyor. Bu alan gerekli oldu çünkü **iki ayrı yanlış varsayım gerçek verilerle çürütüldü**:

1. `productCode`'un ilk hanesi sabit `0` değil, ürün kategorisine göre değişiyor (giyimde `0`, ayakkabıda `1` görüldü — REF `1032/864/100` çizme → gerçek partnumber `1103286410035-I2026`).
2. Alfabetik bedenlerde (XS/S/M/L) part-number'ın son 2 hanesi bedenin kendisinden değil, **ürüne özgü bir sıra numarasından** geliyor (tahmin edilemez).

Tek güvenilir kaynak: ürünün kendi sayfasında Bershka'nın zaten hesapladığı gerçek `partnumber`/`stock` bilgisi — bu da sayfanın URL'sini gerektiriyor.

#### Ürün sayfasından (PDP) gerçek stok verisi okuma

Ürün sayfasının SSR HTML'ine gömülü minified JS nesnesinde (`colors[].sizes[]`) Bershka'nın **her beden için** zaten hesapladığı gerçek veriler var — tam olarak sitede "beden seçildiğinde disabled görünme" davranışının kaynağı (ör. `{stock:"in_stock",name:"XS",types:[{partnumber:"0332736067601-I2026",mastersSizeId:"101"}]}`).

**İlk deneme (terk edildi) — statik HTML'i regex ile ayrıştırma**: Chrome kanalıyla çekilen HTML'e karşı bir regex (`SizeEntryRegex`) çalıştırılıyordu. Bu, jean/çizme/bazı top'larda çalıştı ama **çiçekli korse askılı top**'ta (REF 3327/360/676, kullanıcının bildirdiği ürün) tutarlı şekilde başarısız oldu — canlı HTML incelendiğinde nedeni bulundu: bu sayfada değerler literal string olarak değil (`stock:"in_stock"`), **paylaşılan değişken referansı** olarak geliyordu (`stock:am`, gerçek değer `am="in_stock"` sayfanın başka bir yerinde tanımlı). Bu, bundler'ın string tekilleştirme (deduplication) optimizasyonu — hangi sayfada tetikleneceği dışarıdan öngörülemez, bazı ürünlerde oluyor bazılarında olmuyor. Statik metinde gerçek değer hiç yazılı olmadığından regex'i "düzeltmek" mümkün değildi.

**Nihai tasarım — Vue component ağacından JS çalışma zamanı okuma**: `PlaywrightPdpFetcher.FetchProductSizesJsonAsync`, sayfa yüklendikten sonra `document.getElementById('__nuxt').__vue__` kökünden başlayıp `$children` ağacını (sınırlı derinlik/ziyaret sayısıyla, ~600-1000 component tipik) tarar; `$data`/`$props` içinde `colors[].sizes[]` barındıran component'i bulup `page.EvaluateAsync` ile **JS'in kendi çalışma zamanında çözümlediği gerçek değerleri** okur. Bu, minifier'ın string'i literal mi değişkene mi çıkardığından tamamen bağımsız çalışır — Vue'nun reaktif state'i her zaman gerçek JS değerini tutar, kaynak kodun görünümü onu etkilemez. Sonuç doğrudan `List<SizeEntry>` (Name, Stock, PartNumber, MastersSizeId) şeklinde JSON olarak C# tarafına dönüyor; `BershkaStockApiClient` artık hiçbir regex/HTML ayrıştırması yapmıyor.

`BershkaStockApiClient`, verilen `productCode`'un **son 3 hanesinden çıkardığı `ColorId`** (PDP'nin kendi `color.id` alanıyla, aile/partnumber öneki bağımsız — bkz. altta neden StartsWith yerine bu kullanılıyor) ve beden adına (case-insensitive **string eşleşmesi** — "36" da "XS" de aynı şekilde çalışır) sahip kaydı bulur:
- **Online stok**: o bedenin `Stock` alanı `"in_stock"` mı? (`"coming_soon"` ve `"out_of_stock"` — üçüncü bir değer, canlı veride keşfedildi — ikisi de `false` sayılır).
- **Mağaza stok**: o bedenin gerçek `PartNumber`'ı (campaign dahil, `-` ile ayrılıyor) ile `api.inditex.com/.../stock/campaign/{campaignId}/product/part-number/{digits}?physicalStoreId=...` sorgulanır; dönen `sizeStocks[]` içinden `MastersSizeId`'ye eşit **`size`** alanı filtrelenir (tek çağrı, mağaza başına TÜM bedenlerin stoğunu döndürüyor — filtrelemezsek yanlış bedenin adedini okumuş oluruz).

> #### ⚠️ Kritik hata bulundu ve düzeltildi — `sizeId` ≠ `size`
> Kullanıcının 10 gerçek üründe canlı testi çapraz doğrulaması sırasında bulundu: stok API'sinin yanıtındaki `sizeStocks[]` dizisinde **`sizeId` ve `size` farklı alanlar**:
> ```json
> {"sizeId": 3, "size": 103, "quantity": 4}
> ```
> `sizeId`, mağaza içindeki bedenlerin sıralı/anlamsız bir indeksi (1,2,3,4...); `size` ise PDP'den okunan `MastersSizeId` ile birebir eşleşen gerçek beden kodu. İlk implementasyon `sizeId`'ye göre filtreliyordu — sayısal bedenlerde (jean: 32,34,36) `sizeId` ve `size` tesadüfen eşit çıktığı için bu fark edilmedi, ama alfabetik/3-haneli `mastersSizeId` sistemi (101,102,103...) kullanan ürünlerde `sizeId=3` hiçbir zaman aranan `103`'e eşit olmadığından **gerçek stok varken sürekli yanlışlıkla `OutOfStock` dönüyordu**. Kullanıcı bunu, uygulamanın söylediği ile sitede/mağazada bizzat gördüğü arasındaki farkı bildirerek yakaladı (bkz. Faz 2.4 canlı doğrulama). `SizeStockDto`'ya `Size` alanı eklenip filtreleme buna göre düzeltildi; regresyon testi eklendi.

> #### Boş mağaza yanıtı artık `Unknown`, `OutOfStock` değil
> Bazı ürünler mağaza stok özelliğini hiç desteklemiyor (sitede "Mağazada mevcudiyet" bölümü bile görünmüyor) — bu durumda stok API'si `{"stocks":[]}` döner. Bunu `OutOfStock` saymak yanlış ve zararlı: kullanıcı ürünü mağazada gerçekten görebilir, biz sadece o mağaza için hiç veri alamamışızdır — "bilmiyorum" demek, yanlış "yok" demekten daha güvenli (kullanıcının deneyimini korur). `CheckStoreStockAsync` artık: (1) `sizeStocks` tamamen boşsa, (2) hedef beden yanıtta hiç bulunamazsa → `Unknown` (`null`) döner; yalnızca gerçek bir `sizeStocks` kaydı bulunup adedi sıfırsa `OutOfStock` döner.

> #### ⚠️ Kritik hata bulundu ve düzeltildi — bir renk, birden fazla partnumber "ailesi"ne bölünmüş olabiliyor
> 3. tur canlı testinden sonra kullanıcı, uygulamanın `OutOfStock`/`Unknown` dediği 2 üründe (ürün 2, ürün 8) ürünü bizzat sitede/mağazada **stokta** gördüğünü bildirdi. Kök neden, ürün 2 ("Kısa kollu poplin bağlamalı gömlek", "Haki" rengi) üzerinde derinlemesine incelenip doğrulandı:
> - Aynı `ColorId` (renk) içindeki bedenler **farklı partnumber öneklerine** ("aile") dağılmış olabiliyor — ör. Haki'de XS/S/L bedenleri `01337711507` ailesinden, M/XL ise **tamamen farklı** `01337015507` ailesinden geliyordu. İkisi de gerçek, bağımsız stoklanan SKU kayıtları — Bershka/Inditex tarafında bir parti/batch ayrımı.
> - Sitenin kendi "Mağazada mevcudiyet" akışı, Playwright ile ağ trafiği yakalanarak izlendiğinde, **her iki aileyi de aynı beden-konum koduyla paralel sorgulayıp** (ör. `part-number/0133771150701` VE `part-number/0133701550701`, aynı 9 mağaza için) sonuçları birleştirdiği görüldü.
> - Eski implementasyon sadece hedef bedenin KENDİ partnumber'ını (tek bir aile) sorguluyordu — beden diğer ailede kayıtlıysa (PDP feed'indeki aile ataması güvenilir değil) o ailenin o mağazadaki gerçek stoğu tamamen kaçırılıyordu.
> - Ayrıca, eski `ResolveSizeEntryAsync` bir bedeni bulmak için `entry.PartNumber.StartsWith(productCode)` kontrolü yapıyordu — bu da AYNI hatanın bir başka yüzüydü: caller'ın gönderdiği `productCode` sadece BİR aileyle prefix-eşleşiyordu, o ailede bulunmayan bedenler (ör. M/XL, `01337711507` ailesinde hiç yok) için **hiç eşleşme bulunamıyor**, sessizce `Unknown` dönüyordu.
>
> **Düzeltme**: (1) beden eşleştirmesi artık partnumber prefix'i yerine PDP'nin kendi `color.id` alanından türetilen `ColorId`'ye göre yapılıyor (aile ayrımından bağımsız, güvenilir); (2) `CheckStoreStockAsync`, hedef bedenin kendi konum kodunu (partnumber'ın son 2 hanesi) aynı `ColorId`'ye ait TÜM bilinen aile öneklerine uygulayıp hepsini paralel sorguluyor, dönen adetleri topluyor — sitenin kendi davranışıyla birebir aynı. Regresyon testleri eklendi (`ResolveSizeEntry_MatchesByColorIdNotPartNumberPrefix_...`, `CheckStoreStockAsync_WhenColorHasMultiplePartNumberFamilies_...`).

Bu yaklaşım kategoriye/beden sistemine göre dallanma **gerektirmiyor** — Bershka'nın kendi hesapladığı veriyi okuyoruz, kendimiz kod/id üretmiyoruz.

> **Değerlendirilip vazgeçilen alternatif**: DOM'daki beden butonlarının `aria-disabled` niteliği de (`size-button--disabled` class'ı, `aria-description="Yakında stokta olacak!"`) online stok sinyalini güvenilir şekilde veriyor — ama üzerinde `partnumber` gibi bir `data-*` niteliği yok, yani mağaza-bazlı kontrol için tek başına yeterli değil. Vue ağacı taraması hem online hem mağaza ihtiyacını tek kaynaktan karşıladığı için tercih edildi.

#### Playwright + Redis cache-aside — neden gerekli

PDP'ler Akamai Bot Manager'ın arkasında. Prodüksiyona karşı doğrulanan üç ayrı katman:

1. **Çerezsiz `curl`** (herhangi bir UA ile) → tutarlı şekilde ~2.3KB'lık bir JS interstitial (proof-of-work challenge) sayfası döner, gerçek içerik yok. Stok/mağaza-bulucu JSON API'leri bundan etkilenmiyor.
2. **Playwright'ın varsayılan (bundled) Chromium'u** → UA/`navigator.webdriver` maskeleme gibi standart stealth önlemlerine rağmen Akamai'den **anında "Access Denied" (403)** alındı (interstitial bile değil, sert bir edge-level red). Kök neden kesin olarak izole edilemedi (TLS/fingerprint farkı olası).
3. **Gerçek Chrome kanalı** (`BrowserTypeLaunchOptions.Channel = "chrome"`, Playwright'ın kendi Chromium'u değil, makinedeki gerçek Google Chrome) → engeli aştı, gerçek içerik döndü. `WaitUntilState.NetworkIdle` bu sayfada hiç tetiklenmediği için (muhtemelen sürekli arka plan telemetri isteği) `DOMContentLoaded` + sabit bir bekleme kullanılıyor; ağır ürün sayfalarında (çok renk/beden) tüm veri hydrate olması **8 saniye** sürebiliyor (3 saniye yetersiz kaldı, gerçek veriyle doğrulandı — jean sayfasında 7 bedenden yalnızca 1'i gelmişti).

- **`IBershkaPdpFetcher` / `PlaywrightPdpFetcher`**: gerçek Chrome kanalını headless çalıştırıp JS'i işleterek Akamai'nin challenge'ını bir tarayıcı gibi geçer. Chrome process'i pahalı olduğu için singleton olarak bir kez başlatılır, her istekte yeni bir `BrowserContext`/`Page` açılıp kapatılır (izolasyon, process yeniden başlatılmadan).
- **Redis cache-aside** (`BershkaStockApiClient`, key: `bershka:pdp-sizes:{productUrl}`, TTL 15 dk): bir PDP çekimi ürünün **tüm bedenlerinin** verisini tek seferde verdiği için, aynı ürüne yapılan art arda aramalar (farklı il/ilçe, farklı kullanıcı, hem online hem mağaza kontrolü) Playwright'ı tekrar tetiklemez. Bu, hem gecikmeyi (çoğu istek cache'den milisaniyelerde döner) hem de Akamai'nin hacimle biriken "bot itibarı" riskini azaltır (bkz. altta Ölçeklenme Riski). Boş sonuç cache'lenmez — geçici bir hata olabilir, bir sonraki istek tekrar dener.
- **Kurulum gereksinimi**: Playwright'ın .NET paketi tarayıcı binary'sini indirmez — her geliştirici makinesinde/deploy ortamında bir kez `playwright install chrome` (bundled `chromium` değil, `chrome` kanalı) çalıştırılmalı. `bin/` gitignore'da olduğu için bu adım repo klonlandığında görünmez — bkz. `.claude/ENVIRONMENT_SETUP.md` → "Bershka Scraper — Playwright/Chromium Kurulumu".
- **Alfabetik bedenler destekleniyor**: eski tasarımda (`TryBuildSizeId`) sadece sayısal bedenler kabul ediliyordu; artık PDP'den gerçek veri okunduğu için hem sayısal hem alfabetik bedenler aynı kod yoluyla çalışıyor.

**Playwright'ın güvenilirliği hakkında dürüst bir not** (kullanıcıyla konuşuldu, bkz. altta Ölçeklenme Riski): headless/otomasyon tespiti ve hacim arttıkça düşen başarı oranı kalıcı riskler — cache-aside bunu *azaltır*, ortadan kaldırmaz. CAPTCHA'ya eskalasyon durumunda (kapsam dışı, çözülmeye çalışılmıyor) sonuç `Unknown` olur. **Çerez/oturum yeniden kullanımı** (Playwright'ın çözdüğü Akamai çerezini bir süre plain `HttpClient` ile tekrar kullanmak, Playwright çağrısını daha da seyrekleştirmek için) bilinçli olarak **şimdilik eklenmedi** — cache-aside'ın yeterliliği görüldükten sonra gerekirse eklenecek bir sonraki optimizasyon olarak not edildi.

**Somut örnek — gerçek geçici timeout**: 10 ürünlük ikinci canlı test turunda bir üründe `CheckOnlineStockAsync` beklenmedik şekilde `Unknown` döndü. Kök neden araştırıldı: aynı ürün 3 kez art arda çekildi, dönen veri (renk/beden/stok) **tamamen tutarlıydı** — yani bir veri tutarsızlığı değildi. Ayrı bir denemede ise `page.GotoAsync` gerçekten **30 saniyelik navigasyon timeout'una** takıldı. Yani ara sıra yaşanan `Unknown` sonuçları çoğunlukla Playwright'ın kendi geçici ağ/render yavaşlığından kaynaklanıyor — sistem bunu doğru şekilde `Unknown`'a çeviriyor (yanlış `true`/`false` uydurmuyor), ama bu, prodüksiyonda belirli bir oranda beklenmesi gereken, normal bir davranış.

**Canlı doğrulama — tamamlandı, beş tur**: `PlaywrightPdpFetcher`/`BershkaStockApiClient`, geliştirici makinesinde gerçek Chrome kurulduktan sonra bizzat çalıştırılıp **toplam 40+ farklı gerçek üründe** (çeşitli kategoriler: jean, top, elbise, etek, pantolon, gömlek, şort, sweatshirt, hırka, bot, spor ayakkabı) canlı Bershka sitesine, gerçek mağazalara (Kadıköy/Şişli/Çankaya/Bornova rotasyonlu) ve gerçek `api.inditex.com` stok API'sine karşı test edildi:
- **1. tur (4 ürün, sadece online)**: 7/7 sonuç sitedeki gerçek durumla birebir örtüştü (kullanıcının bildirdiği "XS stokta, S/M/L değil" senaryosu dahil).
- **2. tur (10 ürün, online + mağaza)**: kullanıcı sonuçları sitede/mağazada bizzat kontrol edip 3 üründe (6, 7, 8) tutarsızlık bildirdi — bu, yukarıdaki `sizeId`/`size` hatasının bizzat kullanıcı tarafından yakalanmasını sağladı. Düzeltme sonrası **3. tur (10 yeni ürün)** ile yeniden doğrulandı; ayrıca bir üründeki beklenmeyen `Unknown` sonucu ayrıca araştırılıp gerçek bir geçici Playwright timeout'u olduğu (veri tutarsızlığı değil) kanıtlandı.
- **4. tur (3. turdaki sonuçların kullanıcı tarafından yeniden çapraz doğrulanması)**: kullanıcı 2 üründe (2, 8) yine tutarsızlık bildirdi — bu, yukarıdaki "bir renk, birden fazla partnumber ailesi" hatasının bulunup düzeltilmesini sağladı (ürün 2 üzerinde Playwright ile sitenin kendi ağ trafiği yakalanarak kök nedeni kanıtlandı). Diğer iki üründe (3, 9) kullanıcının "beden bilgisi olmadan aratılmış" gözlemi araştırıldı; PDP'nin Vue verisi ve DOM beden butonları her iki üründe de test edilen bedenin geçerli ve aktif olduğunu doğruladı.
- **5. tur (`ColorId`/çoklu-aile düzeltmesi sonrası, 10 yeni ürün)**: üretimdeki `BershkaStockApiClient`/`PlaywrightPdpFetcher` sınıfları doğrudan (yalnızca Redis cache mock'landı — aynı PDP'nin iki kez Playwright ile çekilmesini önlemek için) top/jean/elbise kategorilerinden 10 üründe, her birinde 2 farklı beden × 2 farklı gerçek mağaza olacak şekilde çalıştırıldı; toplam 20 online + 40 mağaza kontrolü gerçek stok API'sine karşı yapıldı. Bazı ürün/mağaza kombinasyonlarında beklenen ve doğru `Unknown` (o mağaza için stok verisi hiç dönmeyen ürünler) gözlendi, önceki turlarda bulunan hata sınıflarından hiçbiri tekrarlanmadı. **Kullanıcı sonuçları doğru olarak onayladı.**

Kod, aynı veri şekillerini taklit eden JSON mock'larla 24 unit testle kapsanıyor (`sizeId`≠`size` regresyonu, boş-mağaza-yanıtı→`Unknown`, `ColorId` bazlı eşleştirme ve çoklu-aile toplama testleri dahil).

### Scraper Health Monitoring (Faz 2.5 — Tamamlandı)

Her deneme (Playwright PDP çekimi, stok API'sine giden her HTTP isteği) başarılı/başarısız olarak, HTTP durum koduyla birlikte loglanır — amaç, bir marka scraper'ının sessizce bozulmaya başladığını (ör. Akamai tespiti sıkılaştı, oran %200→%403'e kaydı) erken yakalamak.

> #### Tasarım kararı: ayrı bir Postgres DB yerine paylaşılan Redis
> İlk tasarım `ScraperHealthLog`'u kendi Postgres tablosu olan yeni bir `bershka_scraper_db` olarak öngörüyordu (proje genelinde "database-per-service" prensibiyle tutarlı olsun diye). Kullanıcı bunu haklı olarak sorguladı: **birden fazla marka scraper'ı** (Faz 6.1'de Zara, Pull&Bear) geldiğinde her biri için ayrı bir DB açmak (docker-compose girişi, migration, connection string — N marka için N kez tekrar) hem gereksiz operasyonel yük hem de merkezi görünürlüğü ("hangi scraper şu an başarısız oluyor" gibi tek bir soruya cevap vermek için N farklı DB'yi sorgulamak gerekir) zorlaştırır. `ScraperHealthLog` aslında Bershka'ya özel bir domain verisi değil — tüm scraper'larda birebir aynı şekilde tekrarlanacak operasyonel telemetri. Proje zaten Search Orchestrator throttle'ı ve Bershka PDP cache'ini tam bu gerekçeyle (domain değil, operasyonel/cross-cutting veri) paylaşılan tek Redis instance'ında tutuyor — health log de aynı kategoriye girer. **Karar**: yeni bir DB açılmadı; `scraper:health:{scraperName}:log` key'inde (scraper adına göre namespace'lenmiş) bir capped-list (`LPUSH` + `LTRIM` ile son 500 deneme) kullanılıyor.

- **`StockTracker.Shared.Scraping`** (yeni paylaşılan proje — `.claude/ARCHITECTURE.md` > Ölçeklenme Riski'nde öngörülen "paylaşılan scraping altyapısı"nın ilk parçası): `IScraperHealthLogService`/`ScraperHealthLogService`. `LogAttemptAsync(scraperName, source, success, httpStatusCode, errorMessage, durationMs)` her denemeyi Redis'e yazar — **asla exception fırlatmaz** (Redis'e erişilemese bile stok kontrolü sonucu kullanıcıya dönmeye devam etmeli, sağlık loglaması ikincil bir kaygı). `GetStatsAsync(scraperName, lastN)` son N denemeyi okuyup `SuccessRatePercent`, `HttpStatusCodeDistribution` (ör. `{"200":18,"403":2}`) ve `AlertTriggered` hesaplar.
- **Alarm mekanizması**: örneklem en az 10 olmadan (soğuk başlangıçta tek bir geçici hatanın "başarı oranı çöktü" alarmını tetiklememesi için) ve başarı oranı %70 eşiğinin altına düştüğünde `AlertTriggered=true` döner + `ILogger` üzerinden bir Warning log basılır. Ayrı bir bildirim kanalı (Slack/e-posta) şu an yok — proje genelinde henüz böyle bir altyapı yok, bu yüzden yapılandırılmış log, bu fazın kapsamındaki en gerçekçi "alert" biçimi.
- **Entegrasyon noktaları**: `PlaywrightPdpFetcher.FetchProductSizesJsonAsync` (`source: "PlaywrightPdp"`, `httpStatusCode` gerçek `page.GotoAsync` yanıtının `Status`'undan, `context: productUrl`) ve `BershkaStockApiClient.CheckStoreStockAsync`'in her aile-prefix'i için attığı stok API isteği (`source: "StockApi"`, `context: "{productUrl} | store={id} partNumber={digits}"`).
- **`Context` alanı**: kullanıcı ("hangi urlde hata alındığını anlamlandıralım") talebiyle eklendi — her log kaydı artık hangi ürün/mağaza/partnumber üzerinde olduğunu insan tarafından okunabilir şekilde taşıyor; `redis-cli LRANGE` ile ham JSON'a bakıldığında bile doğrudan anlamlı.
- **Görüntüleme**: `GET /health/scraper-stats?lastN=100` — `{ scraperName, sampleSize, successRatePercent, httpStatusCodeDistribution, alertTriggered }` döner. `GET /health/scraper-failures?lastN=20` — `GetRecentFailuresAsync` ile son N BAŞARISIZ denemeyi (Context + ErrorMessage dahil) döner, Redis'e elle bakmadan "hangi üründe hata alındı" sorusuna cevap verir.
- Gerçek Chrome + gerçek Redis + gerçek `api.inditex.com` ile doğrulandı (4 gerçek ürün + 1 kasıtlı bozuk URL): PDP çekimi başarısızlıklarının cache'lenmemesi tasarım kararı, health log'da aynı bozuk URL için tekrarlanan gerçek 404 kayıtları olarak (online + mağaza kontrolünün ikisi de ayrı ayrı denediği için) doğru şekilde görünüyor — Redis'teki ham kayıt ile servisin hesapladığı istatistikler birebir tutarlı bulundu.
- 12 unit test (`tests/StockTracker.Shared.Scraping.Tests`): başarı oranı/durum kodu dağılımı hesaplama, küçük örneklemde alarm tetiklenmemesi, Redis hatası durumunda exception fırlatılmaması, farklı scraper isimlerinin bağımsız key kullanması, `GetRecentFailuresAsync`'in yalnızca başarısızları context'iyle birlikte döndürmesi dahil.

### Subscription Service — Watch Group Modeli (Faz 3.1 — Tamamlandı)

- `POST /watches` — `{userId, productCode, size, storeId?}` alır. Önce `WatchGroups`'ta aynı `ProductCode`+`Size`+`StoreId` var mı bakılır; yoksa oluşturulur. Ardından o kullanıcının bu `WatchGroup`'a ait `UserWatch`'ı var mı bakılır (yoksa oluşturulur) — bu iki adımlı akış dedup'ın kendisi: aynı ürünü farklı kullanıcılar takip ettiğinde tek `WatchGroup`, kullanıcı başına ayrı `UserWatch` satırı oluşur.
- `GET /watches?userId=` — kullanıcının takip listesini `WatchGroup`'un güncel `LastKnownStatus`/`LastCheckedAt` bilgisiyle birlikte döner (Faz 3.2'deki poller bu alanları güncelleyecek).
- `DELETE /watches/{id}?userId=` — `id`, `UserWatch.Id`'dir (WatchGroup değil). `userId` sahiplik doğrulaması için zorunlu query param; eşleşmezse `404` döner (kayıt bulunamadı ile başkasına ait olma durumu ayrıştırılmaz, bilgi sızıntısı önlenir). Silme yalnızca o kullanıcının takibini kaldırır — `WatchGroup` başka kullanıcılar tarafından hâlâ takip ediliyor olabileceğinden silinmez, orphan `WatchGroup` bilinçli olarak temizlenmiyor (Faz 3.2 poller'ı boşuna kontrol eder ama veri kaybı riski yaratmaz; gerekirse ileride bir cleanup job'ı eklenebilir).
- **Dedup DB seviyesinde değil, application seviyesinde**: `WatchGroups(ProductCode, Size, StoreId)` üzerinde unique olmayan bir index var — `StoreId` nullable olduğundan Postgres'te unique constraint NULL'ları ayrı kayıt sayardı (iki online-only watch aynı ürün/beden için farklı satır açardı), bu yüzden dedup `WatchService.CreateWatchAsync` içinde "find or create" ile yapılıyor. `UserWatches(UserId, WatchGroupId)` ise gerçekten unique — aynı kullanıcının aynı gruba iki kez eklenmesi DB seviyesinde engelleniyor.
- **Gerçek Postgres'e karşı bulunan hata**: `GetWatchesAsync` ilk implementasyonda `OrderByDescending`'i DTO projeksiyonundan sonra uyguluyordu. InMemory test provider'ı (unit testlerde kullanılan) bunu client-eval ile sessizce çalıştırdı, ama gerçek Npgsql sağlayıcısı projeksiyon (DTO constructor çağrısı) içindeki alanla sıralamayı SQL'e çeviremeyip `InvalidOperationException` fırlattı — `dotnet test` yeşildi ama gerçek servis `GET /watches` çağrısında 500 dönüyordu. Docker Postgres'e karşı uçtan uca smoke-test sırasında yakalanıp düzeltildi (`OrderByDescending` artık `Join`'den önce, ham `UserWatches` üzerinde çalışıyor) — bu, "unit testler InMemory provider ile SQL çevirisi hatalarını yakalayamayabilir" örneği olarak not edildi.
- Gerçek Docker Postgres'e (`subscription_db`) karşı uçtan uca doğrulandı: iki farklı kullanıcının aynı ürün/beden/mağazasız kombinasyonu takip etmesi tek `WatchGroup`'a dedup edildi, sahiplik dışı silme denemesi `404`, sahibinin silmesi `204` ve takip listesini boşalttı — sonrasında test verisi temizlendi.
- Unit testler `tests/StockTracker.Subscription.Tests` — 9 test (yeni `WatchGroup` oluşturma, iki farklı kullanıcının aynı gruba dedup edilmesi, aynı kullanıcının tekrar eklemesinin duplicate yaratmaması, farklı `StoreId`'lerin ayrı grup açması, kullanıcı bazlı listeleme, `LastKnownStatus`/`LastCheckedAt` alanlarının doğru taşınması, sahiplik kontrollü silme senaryoları dahil).
- **İç iletişim (Faz 3.3)**: `GET /internal/watchers?productCode=&size=&storeId=` — gateway bypass, direkt HTTP. Notification Service, bir restock event'inde kime bildirim gideceğini (o `WatchGroup`'u takip eden tüm `UserId`'ler) buradan çözer.
- **İç iletişim (Faz 4.3)**: `POST /watches` artık yeni bir `UserWatch` oluşturmadan önce Billing Service'in `GET /limits/{userId}`'ini çağırır (`IBillingServiceClient`) — detay için bkz. aşağıda Billing → Limit Kontrol Middleware'i.

### Stock Poller (Faz 3.2 — Tamamlandı)

- **Scheduler**: Quartz.NET (`Quartz.Extensions.Hosting`) — Hangfire yerine tercih edildi çünkü ek bir dashboard/depolama veritabanı gerektirmiyor, `AddQuartzHostedService(opts => opts.WaitForJobsToComplete = true)` ile process-in-process çalışıyor. `StockPollerJob` (`IJob`), `appsettings.json` > `Poller:IntervalSeconds`'ta (varsayılan 60 sn) tanımlı sabit bir `SimpleSchedule` tetikleyicisiyle tekrar tekrar çalışır; job içindeki hata loglanıp yutulur (bir döngüdeki geçici hata sonraki tetiklemeleri etkilemesin diye).
- **Aday seçimi — roadmap'in ötesine bilinçli genişletme**: Faz 3.1 roadmap'i "`LastKnownStatus = OutOfStock` olan WatchGroup'ları çeken job" diyordu, ama gerçek implementasyon `LastKnownStatus IS NULL OR LastKnownStatus <> InStock` filtresini kullanıyor (yani OutOfStock + Unknown + hiç kontrol edilmemiş). Gerekçe: `POST /watches` ile yeni oluşturulan bir `WatchGroup`'un `LastKnownStatus`'u **null**'dur (henüz hiç kontrol edilmedi) — yalnızca literal "OutOfStock" filtrelenseydi bu grup asla ilk kontrolünü almaz, özellik uçtan uca hiç çalışmazdı. `InStock` olan gruplar bilinçli olarak atlanıyor — kullanıcı zaten haberdar edilmiş sayılır (Faz 3.3), tekrar kontrol o ana kadar gereksiz.
- **Önceliklendirme**: `StockPollerService.ResolveCheckInterval(watcherCount)`, o `WatchGroup`'a bağlı `UserWatches` sayısına göre 3 kademeli bir kontrol aralığı döner (varsayılan: ≥5 kullanıcı → 5 dk, ≥2 kullanıcı → 15 dk, aksi halde → 60 dk; hepsi `Poller` config bölümünden okunur). `StockPollerService.IsDue(lastCheckedAt, interval, now)` saf bir fonksiyon — `lastCheckedAt` null'sa (hiç kontrol edilmemiş) veya aradan geçen süre kademenin aralığını aştıysa `true` döner.
- **`CheckStockCommand` (V2) publish akışı**: kontrol zamanı gelen her `WatchGroup` için önce `IProductServiceClient.LookupAsync(productCode)` ile marka/`BrandId`/`ScraperQueueName`/`ProductUrl` çözülür (marka çözülemezse o döngüde atlanır, `LastCheckedAt` **güncellenmez** — bir sonraki döngüde tekrar denenir). `WatchGroup.StoreId` doluysa, `IStoreReferenceServiceClient.GetStoreByIdAsync(storeId)` ile `BrandSpecificStoreId` çözülür — bunun için Store Reference Service'e yeni bir `GET /stores/{id}` endpoint'i eklendi (mevcut `GET /stores` yalnızca liste/filtre destekliyordu, tekil ID sorgusu yoktu). `City`/`District` poller akışında `null` gönderilir — `WatchGroup` bu alanları hiç saklamıyor ve `BershkaStockCheckService`'in yönlendirme mantığı zaten bunlara bakmıyor (yalnızca `ProductUrl`/`StoreId`/`BrandSpecificStoreId` kullanılıyor).
- **Yayın sonrası `LastCheckedAt` güncellemesi — optimistic**: komut publish edilir edilmez `WatchGroup.LastCheckedAt = now` olarak güncellenir (henüz scraper'dan gerçek sonuç gelmeden). Bu, poll interval'i scraper'ın gerçek yanıt süresinden kısaysa aynı grubun art arda döngülerde tekrar tekrar kuyruğa gönderilmesini önlemek için bilinçli bir tasarım kararı — gerçek sonuç geldiğinde (`StockResultEvent`) `LastCheckedAt` zaten event'in kendi `CheckedAt` zaman damgasıyla tekrar (daha doğru şekilde) güncellenir.
- **Kapanan döngü — `StockResultEventConsumer`**: `StockResultEvent` (fanout, `Messages.V1`) yeni bir consumer (`StockResultEventConsumer` → `IWatchGroupStatusUpdater.UpdateFromStockResultAsync`) tarafından tüketilir; gelen event'in `ProductCode`+`Size`+`StoreId` üçlüsüyle eşleşen `WatchGroup` bulunup `LastKnownStatus`/`LastCheckedAt` güncellenir (eşleşme yoksa — ör. bu ürünü henüz kimse takip etmiyor, sadece anlık bir Search Orchestrator sorgusundan gelen sonuç — sessizce yok sayılır). Bu consumer olmadan poller'ın `OutOfStock`/`null` filtresi hiçbir zaman gerçek veriyle beslenmez, döngü hiç kapanmazdı. Receive endpoint adı: `subscription-stock-result-events` (fanout exchange'e bağlı, kendi bağımsız kuyruğu — Notification Service Faz 3.3'te aynı exchange'e kendi kuyruğuyla bağlanacak, ikisi de aynı mesajın bağımsız birer kopyasını alır).
- **Gerçek altyapıya karşı uçtan uca doğrulama**: gerçek Docker Postgres/RabbitMQ + çalışan Product/BrandDetection/StoreReference servislerine karşı yapıldı. Bershka regex'iyle (`^\d{11}$`) otomatik çözülen bir test ürün kodu için `POST /watches` çağrıldı (`LastKnownStatus=null`); 10 saniyelik poller interval'iyle çalışan Subscription servisi bir sonraki döngüde bu grubu aday olarak yakalayıp Product Service'i sorguladı ve `stock.check.bershka` kuyruğuna doğru `CheckStockCommand` V2 payload'ını gönderdi — mesajın gerçek içeriği RabbitMQ Management API üzerinden doğrulandı (doğru `productCode`/`brandName`/`size`, `productUrl: null` çünkü regex-only çözüm). Ardından RabbitMQ Management API ile elle yayınlanan bir `StockResultEvent` (`status: InStock`), `subscription-stock-result-events` kuyruğundaki consumer tarafından tüketilip `WatchGroup.LastKnownStatus`'u `InStock`'a çevirdi; bir sonraki poll döngüsünde bu grubun artık aday listesine girmediği (log: "aday: 0") doğrulandı — döngünün uçtan uca kapandığı kanıtlandı. Sonrasında test verisi (`subscription_db`, `product_db.ProductBrandMaps`, ilgili Redis cache key'i, RabbitMQ'daki test mesajı) temizlendi.
- Unit testler `tests/StockTracker.Subscription.Tests` — `StockPollerServiceTests` (önceliklendirme kademeleri, `IsDue` sınır durumları, InStock/hiç-kontrol-edilmemiş/OutOfStock filtre davranışı, marka çözülemediğinde publish edilmemesi ve `LastCheckedAt`'in değişmemesi, `StoreId`→`BrandSpecificStoreId` çözümü) ve `WatchGroupStatusUpdaterTests` (eşleşen/eşleşmeyen `WatchGroup`, aynı ürün+beden için farklı `StoreId`'li iki grubun birbirinden bağımsız güncellenmesi) — toplam 16 yeni test.

### Notification Service (Faz 3.3 — Tamamlandı, gerçek Firebase/SMTP sunucusu hariç)

- **`StockResultEvent` consumer**: `StockResultEventConsumer` (fanout, `Messages.V1`) kendi bağımsız `notification-stock-result-events` kuyruğuyla dinler — Subscription Service'in Faz 3.2'de eklediği `subscription-stock-result-events` kuyruğundan tamamen ayrı; aynı fanout exchange'in iki bağımsız kopyası, her ikisi de her event'i kendi başına işler.
- **Neden Subscription Service'e "önceki durum neydi" diye sorulmuyor**: ilk tasarım fikri, Notification'ın restock'u tespit etmek için Subscription'daki `WatchGroup.LastKnownStatus`'u okuması olabilirdi, ama bu bir yarış durumu yaratır — aynı `StockResultEvent`'i AYNI ANDA tüketen iki bağımsız consumer var (Subscription'ın kendi `WatchGroupStatusUpdater`'ı ve Notification'ın kendisi); Notification, Subscription'a sorduğu anda Subscription zaten kendi durumunu YENİ değere güncellemiş olabilir, böylece "önceki durum" sorusuna hep yanlış (zaten güncel) cevap alınır. Çözüm: Notification Service, `WatchGroupNotificationState` adında **kendi bağımsız durumunu** tutuyor (Subscription'ın `WatchGroup`'undan tamamen ayrı bir tablo, aynı doğal anahtarla — `ProductCode`+`Size`+`StoreId` — ama farklı bir veritabanında, database-per-service prensibiyle tutarlı). Her event işlendiğinde önce bu tablodaki mevcut durum okunur (= "önceki durum"), sonra event'in durumuna güncellenir — hepsi tek bir `SaveChangesAsync` içinde, aynı consume çağrısında.
- **Geçiş tespiti**: yalnızca `previousStatus == OutOfStock && yeniStatus == InStock` ise bildirim tetiklenir (`NotificationProcessingService.ProcessAsync`). İlk kontrol (previousStatus `null`, yani hiç bilinmiyordu) `InStock` gelse bile bildirim **göndermez** — kullanıcı hiçbir zaman "yok" olduğunu öğrenmediği için bu bir "tekrar stokta" haberi sayılmaz, roadmap'in "sadece yok→var" ifadesiyle birebir örtüşüyor.
- **Kime bildirim gidecek**: Subscription Service'e yeni eklenen `GET /internal/watchers?productCode=&size=&storeId=` (gateway bypass, direkt HTTP) ile o `WatchGroup`'u takip eden tüm `UserId`'ler çözülür.
- **Email (kendi SMTP sunucumuz) — gerçekten çalışır durumda**: kullanıcı kararıyla 3. taraf bir email sağlayıcısı (SendGrid/Postmark/SES vb.) **kullanılmıyor** — `SmtpEmailSender` (MailKit ile, .NET'in kendi `SmtpClient`'ı Microsoft tarafından yeni geliştirme için önerilmediğinden) kendi SMTP sunucumuza/relay'imize gerçek bir bağlantı kurup gönderir. Kullanıcının email adresi, Identity Service'e eklenen `GET /internal/users/{id}` (gateway bypass) ile gerçek zamanlı çözülür. Kendi mail altyapımız henüz kurulmadığı için (bkz. `.claude/PENDING_INPUTS.md`) `SMTP_HOST` `.env`'de placeholder (`REPLACE_WITH_ENV`) — bu durumda `SmtpEmailSender` bağlantı denemeden `false` döner ve loglar, servis çökmez. Gerçek bir SMTP sunucusu girildiğinde kod değişikliği gerekmeden çalışır — bu, gerçek bir yerel SMTP sunucusuna (MailHog, Docker) karşı canlı olarak doğrulandı (bkz. altta).
  - `SmtpSettings.IsConfigured`, yalnızca `Host`'un dolu olmasını arar — `Username`/`Password` bilinçli olarak zorunlu değil, çünkü bazı SMTP relay'leri (ör. yerel ağdaki kimlik doğrulamasız bir mail sunucusu) auth gerektirmeyebilir.
- **Push (FCM) — altyapı hazır, bu fazda kullanılmıyor**: `FcmPushSender`, FCM'in legacy HTTP API'sine (`fcm.googleapis.com/fcm/send`, `Authorization: key=<FCM_SERVER_KEY>`, roadmap'in "Server Key al" ifadesiyle birebir eşleşen, OAuth2/service-account gerektirmeyen basit yöntem) gerçek bir HTTP client olarak yazıldı. Ama pratikte **hiç çağrılmıyor**: sistemde hiçbir yerde kullanıcı başına bir push/cihaz token'ı saklanmıyor (mobil uygulama henüz yok — token kaydı Faz 5.4'ün kapsamı: "FCM push notification token kaydı"). `IUserDeviceTokenProvider`'ın şu anki tek implementasyonu `NoOpDeviceTokenProvider`, her zaman `null` döner; `NotificationProcessingService` bunu görünce push kanalını sessizce (ama loglayarak) atlar, `NotificationLog`'a hiç kayıt açmaz (gerçekten denenmedi). Faz 5.4'te gerçek bir token kaynağına bağlanan bir implementasyonla değiştirilecek.
- **Idempotency**: `NotificationLog` üzerinde `(CommandId, UserId, Channel)` unique index + gönderim öncesi `AnyAsync` kontrolü — MassTransit'in at-least-once teslimat garantisiyle aynı event iki kez tüketilse bile (ör. broker yeniden başlatma sonrası redelivery) aynı kullanıcıya aynı kanaldan ikinci bir bildirim gitmez. Ayrıca durum geçişi mantığının kendisi de doğal bir idempotency katmanı sağlıyor: `WatchGroupNotificationState` bir kez `InStock`'a güncellendikten sonra aynı event'in tekrar işlenmesi zaten "restock" olarak görülmez (`previousStatus` artık `InStock`).
- **Kullanıcı kararı — email için 3. taraf sağlayıcı kullanılmıyor (şimdilik)**: ilk implementasyon SendGrid kullanıyordu (roadmap'te öyle geçtiği için), ama kullanıcı sonradan bunu değiştirdi — "ilk süreçlerde bu servisi almak istemiyorum", email kendi SMTP sunucumuz üzerinden gönderilecek. `SendGridEmailSender` tamamen kaldırılıp yerine `SmtpEmailSender` (MailKit) yazıldı; `IEmailSender` arayüzü sayesinde bu değişiklik yalnızca `Program.cs`'teki DI kaydı ve bir yeni dosya ile sınırlı kaldı, `NotificationProcessingService`/consumer/idempotency mantığı hiç değişmedi. FCM için ayrı bir karar yok — Firebase hesabı hâlâ bekleniyor (bkz. `.claude/PENDING_INPUTS.md`), `FcmPushSender` aynı şekilde placeholder'la duruyor.
- **Planlanan sonraki adım — Amazon SES'e geçiş**: kullanıcı, kendi SMTP sunucusunu geçici bir başlangıç noktası olarak görüyor — ileride email gönderimi Amazon SES'e taşınacak (bkz. `.claude/PENDING_INPUTS.md`). `IEmailSender` soyutlaması bu geçişi küçük bir iş haline getiriyor: SES'in SMTP arayüzü kullanılırsa (`email-smtp.<region>.amazonaws.com`) mevcut `SmtpEmailSender`/MailKit neredeyse hiç değişmeden çalışır; API tabanlı gitmek istenirse `AWSSDK.SimpleEmailV2` ile yeni bir `IEmailSender` implementasyonu eklenir. Henüz kod değişikliği yapılmadı — bu yalnızca bir planlama notu.
- **Gerçek altyapıya karşı uçtan uca doğrulama**: gerçek Docker Postgres/RabbitMQ + çalışan Identity/Subscription/Notification servislerine karşı yapıldı. Identity'de gerçek bir kullanıcı kaydedildi, Subscription'da bu kullanıcı adına bir `WatchGroup` (`POST /watches`) oluşturuldu. RabbitMQ Management API ile elle önce `OutOfStock` sonra `InStock` `StockResultEvent`'i yayınlandı — Notification Service `GET /internal/watchers` ile doğru kullanıcıyı, `GET /internal/users/{id}` ile gerçek email adresini çözdü; ilk turda (SMTP henüz yapılandırılmamışken) `SmtpEmailSender` `false` döndü ve bu `NotificationLog`'a (`Channel=Email, Success=false`) doğru yazıldı. **SMTP gönderim mekanizmasının kendisi de ayrıca canlı doğrulandı**: Docker'da geçici bir MailHog (yerel SMTP test sunucusu) ayağa kaldırılıp `.env`'e gerçek bağlantı bilgileri (`SMTP_HOST=localhost`, `SMTP_PORT=1025`, auth'suz) girildi, aynı restock akışı tekrarlandı — MailHog'un API'sinde doğru `To`/`From`/`Subject` (Türkçe karakterler dahil, UTF-8 doğru encode edilmiş) ile gerçek bir email yakalandı. Push kanalının token yokluğu nedeniyle hiç `NotificationLog` satırı açmadan atlandığı doğrulandı. Aynı restock event'i tekrar yayınlandığında (durum zaten `InStock` olduğu için) ikinci bir `NotificationLog` satırı açılmadığı doğrulandı — sonrasında test verisi (`notification_db`, `subscription_db`, Identity'deki test kullanıcısı, MailHog container'ı) temizlendi, `.env` placeholder'lara geri alındı.
- Unit testler `tests/StockTracker.Notification.Tests`'e 5 yeni test eklendi: `SmtpSettingsTests` (Host placeholder/null/dolu senaryoları, Username/Password'ün zorunlu olmadığı), `SmtpEmailSenderTests` (yapılandırılmamışken exception fırlatmadan `false` dönmesi).
- Unit testler `tests/StockTracker.Notification.Tests` — 7 test: ilk kontrolde (`previousStatus=null`) bildirim gitmemesi, `OutOfStock→OutOfStock` ve `InStock→OutOfStock` geçişlerinde bildirim gitmemesi, watcher yoksa hiçbir gönderici çağrılmaması, restock'ta her watcher'a email gönderilip `NotificationLog`'a doğru işlenmesi, push token yoksa `IPushSender`'ın hiç çağrılmadan atlanması, idempotency (aynı `CommandId` ile tekrar işlemede duplicate email gitmemesi) dahil.
- **Stok miktarı/son ürün bilgisi bildirim metnine yansıtılıyor (Faz 6.1)**: kullanıcı talebiyle eklendi — "eğer bu bilgiye erişmişsek mutlaka notification'da kullanıcıya belirtelim." `StockResultEvent`'e eklenen `Quantity`/`IsLastUnit` (bkz. yukarı, "Stok Miktarı / Son Ürün Bilgisi" bölümü) `NotificationProcessingService.BuildStockDetailSuffix` ile restock mesajına (hem email hem push, ikisi de aynı `body` string'ini paylaşıyor) ekleniyor: `IsLastUnit=true` ise miktar biliniyorsa "Dikkat: yalnızca N adet kaldı — mağazaya gidene kadar tükenebilir!", bilinmiyorsa (Mango'nun kendi bayrağı) "Dikkat: bu son ürün olabilir — mağazaya gidene kadar tükenebilir!"; yalnızca `Quantity` biliniyorsa (son ürün değilse) "Stokta N adet var."; ikisi de `null`'sa (online kontrol ya da markanın API'si hiç vermiyorsa) hiçbir ek metin eklenmiyor — "bilmiyoruz" ile "bolca stok var" birbirine karıştırılmıyor. Consumer/idempotency/durum-geçişi mantığında hiçbir değişiklik yok, yalnızca mesaj metni zenginleşti.
- Unit testler `tests/StockTracker.Notification.Tests`'e 4 yeni test eklendi (toplam 16): `IsLastUnit=true` + miktar biliniyorken uyarı+miktar birlikte, `IsLastUnit=true` + miktar bilinmiyorken (Mango) yalnızca uyarı, yalnızca `Quantity` biliniyorken (son ürün değil) uyarısız düz miktar metni, ikisi de `null`'ken hiçbir ek metin eklenmemesi.

### Zara Scraper (Faz 6.1 — Tamamlandı)

Bershka'dan (Faz 2.4) sonra ikinci marka — Faz 5 (frontend) tasarım beklerken kullanıcı kararıyla öne alındı. Aynı canlı-doğrulama disiplini uygulandı: hiçbir endpoint/regex/veri şekli tahmin edilmedi, tamamı gerçek `www.zara.com` (TR) trafiği (network capture + doğrudan `curl`) üzerinden keşfedildi.

- **Ürün kodu formatı**: `4 rakam/3 rakam/3 rakam` (ör. `5063/821/802` = `displayReference` + `/` + `colors[].id`). `BrandCodeSignatures` seed'i (`^\d{5}/\d{3}/\d{2,3}$` — hiç doğrulanmamış bir tahmindi) çoklu gerçek örnekle (9083/479, 5372/323, 0962/307, 6224/308, ...) `^\d{4}/\d{3}/\d{3}$` olarak düzeltildi (bkz. `20260804..._FixZaraRegexToFourThreeThree` migration).
- **Online stok**: Zara'nın PDP'si Bershka'daki gibi bir Vue component ağacı değil — veriler `window.zara.viewPayload.product.detail.colors[].sizes[]` içinde **SSR ile doğrudan gömülü**, her bedende bir `availability` alanı (`"in_stock"` = stokta) var. Bershka'nın component-ağacı taraması gerekmiyor, tek bir `page.EvaluateAsync` yeterli. `ColorId` eşleştirmesi (productCode'un son 3 hanesi ↔ `color.id`) Bershka'daki ile aynı gerekçeyle korundu — tek bir PDP birden fazla renk barındırabiliyor.
- **Mağaza bazlı stok — kullanıcının ısrarıyla derinlemesine araştırıldı**: ilk denemelerde sitenin kendi "Mağazadaki stok durumu" arama widget'ı (Google Places tabanlı) güvenilir şekilde tetiklenemedi; kullanıcı bunu ertelemeyi **açıkça reddetti** ("hayır sonraya bırakamayız, mağaza bazlı kısmı için daha detaylı çalışma yap"). Çözüm: sayfa içinde `window.fetch`/`XMLHttpRequest.prototype.open` monkey-patch edilip widget'ın kendi gerçek ağ trafiği yakalandı. Bulunan gerçek endpoint: `GET /tr/tr/store-product-availability?productId={v1}&physicalStoreIds={id}&ajax=true` (tekrarlanabilir `physicalStoreIds` parametresiyle çoklu mağaza tek istekte sorgulanabiliyor).
  - **`productId` = ürün URL'indeki `v1` sorgu parametresi**, sayfanın kendi `product.id` alanından **FARKLI** (canlı veriyle doğrulandı: aynı üründe `product.id=545850110` iken `v1=547843031`, endpoint SADECE `v1` değeriyle çalışıyor). `v1` zaten renk başına değiştiği için mağaza sorgusunda ayrıca `ColorId` eşleştirmesine gerek yok.
  - **Akamai koruması PDP kadar sıkı**: `curl` ile (gerçekçi UA dahil) bu endpoint de 403 döndürüyor — Bershka'nın ayrı, Akamai'siz `api.inditex.com` stok API'si gibi bir şey Zara'da yok. Bu yüzden düz `HttpClient`/Polly pipeline'ı KULLANILMIYOR; sorgu, PDP'ye yapılan bir Playwright navigasyonuyla kurulan (Akamai'yi geçmiş) tarayıcı oturumunun çerezleriyle sayfa içinden (`page.EvaluateAsync` + `fetch`) atılıyor.
  - **Yanıt "seyrek" (sparse) — kritik bulgu**: aynı istekte hem gerçek/var olan bir mağaza (251 — Kentpark/Ankara) hem de stoğu olan bir mağaza (1236) sorgulandığında, yalnızca STOĞU OLAN mağaza dizide döndü; 251 dizide hiç yer almadı. Bu bir hata değil — dizide yer almayan mağaza "bu üründen o mağazada yok" anlamına geliyor. `ZaraStockApiClient.CheckStoreStockAsync` bu yüzden Bershka'nın "boş `stocks[]` = Unknown" davranışının **tersini** uyguluyor: dizide mağaza/beden yoksa `false` (OutOfStock) döner, `null` (Unknown) yalnızca fetch/parse başarısız olursa döner.
  - **Hıza dayalı (velocity-based) Akamai bloklaması — canlı doğrulandı**: birkaç saniye içinde art arda 4-5 istek atıldığında — daha önce ÇALIŞAN bir sorgu bile dahil — TÜM tarayıcı oturumu 403'e düştü, 15 sn beklemeyle bile kendiliğinden düzelmedi (yeni bir context/oturum gerekti). Bu yüzden `PlaywrightZaraFetcher`, mağaza sorgularını tekil eşzamanlılıkla (semaphore) ve aralarında kasıtlı minimum 6 sn beklemeyle sınırlıyor — Bershka'nın host bazlı `HostRateLimitingHandler`'ının HTTP pipeline dışında (Playwright `page.EvaluateAsync` çağrıları için) kendi karşılığı.
- **Servis mimarisi** (`StockTracker.ZaraScraper`, port 5010): Bershka ile aynı iskelet (`CheckStockCommandConsumer`, `stock.check.zara` kuyruğu, `/health/scraper-stats`+`/health/scraper-failures`, `IScraperHealthLogService` entegrasyonu, Redis cache-aside — `zara:pdp-sizes:{productUrl}`, TTL 15 dk, yalnızca online/PDP verisi için; mağaza sorgusu her zaman canlı, önbelleklenmiyor). Tek büyük fark: Bershka'da ayrı bir `HttpClient` + Polly resilience pipeline'ı stok API'sine gidiyordu, Zara'da hem online hem mağaza sorgusu TEK bir `IZaraPdpFetcher` (Playwright tabanlı) üzerinden yapılıyor — ayrı bir stok API'si olmadığı için.
- **Mağaza referans verisi**: `store_db`'ye Bershka'nın mevcut 4 iliyle (Kadıköy/Şişli/Çankaya/Bornova) eşleşecek şekilde gerçek Zara `physicalStoreId`'leri eklendi (Zara'nın kendi `z-maazalar-st1404.html` sayfasından okunan gerçek ID'ler — bkz. `AddZaraStores` migration): Kentpark/Ankara (251, Bershka'nın Kentpark'ıyla aynı AVM), Forum Bornova/Izmir (3643, aynı AVM), Bağdat Caddesi/Kadıköy (3231), Cevahir AVM/Şişli (12692).
- Unit testler `tests/StockTracker.ZaraScraper.Tests` — 22 test: `ZaraStockApiClientTests` (online stok/renk eşleştirme/case-insensitive/cache-aside Bershka desenini mirror ediyor; `CheckStoreStockAsync` için PDP'den `productId` çözümü + URL `v1` fallback'i, sparse yanıtta sorgulanan mağaza/beden dizide yoksa `false` — kritik bulgunun regresyon testi —, fetch başarısızsa `Unknown`), `ZaraStockCheckServiceTests`, `CheckStockCommandConsumerTests` (Bershka'nınkilerle birebir aynı şekil).

**Canlı doğrulama — ikinci tur (kullanıcı talebiyle, 10 ürün × farklı beden × farklı il/ilçe)**: bu ortamda gerçek Chrome kanalı kurulu olmadığı için derlenmiş `StockTracker.ZaraScraper` servisi `dotnet run` ile uçtan uca çalıştırılamadı; bunun yerine üretim kodunun uyguladığı BİREBİR AYNI mantık (aynı endpoint'ler, aynı JSON alan adları, aynı sparse-response kuralı) gerçek bir Chrome tarayıcı oturumu üzerinden 10 farklı gerçek üründe (elbise, jean, gömlek, t-shirt, etek — 5 kategori × 2 ürün) doğrulandı: her üründe online stok (SSR `availability` alanı) okunup 2 beden seçildi, ardından 4 seed'lenen mağazaya (Kadıköy=3231, Şişli=12692, Kentpark/Ankara=251, Forum Bornova/Izmir=3643) TEK istekte (`physicalStoreIds` tekrarlanarak) mağaza stoğu sorgulandı. Bu tur SONUCUNDA BULUNAN VE KODA YANSITILAN ÜÇ GERÇEK BULGU:
1. **`availability` üç değerli** — yalnızca `"in_stock"`/diğerleri değil, üçüncü bir değer `"low_on_stock"` de gözlendi (az sayıda kaldı, ama satın alınabilir). İlk tasarım bunu yanlışlıkla OutOfStock sayıyordu — `ZaraStockApiClient` artık hem `"in_stock"` hem `"low_on_stock"`'u stokta kabul ediyor (bkz. `InStockAvailabilityValues`).
2. **`productId`'nin daha sağlam bir kaynağı bulundu**: PDP JSON'unun kendi `colors[].productId` alanı, URL'in `v1` parametresiyle birebir aynı değeri taşıyor (canlı veriyle doğrulandı: aynı üründe `v1=547843031` ve `colors[{id:"802"}].productId=547843031`) AMA URL'de `v1` olmasa bile mevcut. `PlaywrightZaraFetcher`'ın çıkardığı beden listesine `ProductId` alanı eklendi; `ZaraStockApiClient.CheckStoreStockAsync` artık önce PDP verisinden (ColorId eşleşmesiyle) çözmeyi dener, yalnızca PDP hiç çekilemezse URL'in `v1`'ine geri düşer.
3. **Hız sınırlaması yetersiz kaldı — blok kalıcı**: 8 sn aralıkla (aralarına ürün sayfası navigasyonu da girerek) atılan isteklerde bile 7. istekte oturum yeniden 403'e düştü; ~45-60 sn'lik ek bir bekleme de bunu ÇÖZMEDİ (aynı sorgu tekrar denendiğinde hâlâ 403). Sonuç: 9 mağaza sorgusundan 6'sı `200` (4 mağazaya da veri döndü, biri boş `[]` — genuine "hiçbir mağazada yok" — dahil), 3'ü kalıcı `403` oldu. Bu, sabit bir minimum aralığın (`MinStoreQueryInterval`) bloklamayı yalnızca GECİKTİRDİĞİNİ, KALICI OLARAK ÖNLEMEDİĞİNİ kanıtlıyor — muhtemelen IP/oturum itibarına dayalı, dakikalar sürebilecek bir soğuma gerekiyor. Kod zaten bu durumda güvenli (403 → `FetchStoreAvailabilityJsonAsync` null döner → `ZaraStockApiClient` `Unknown` döner, exception fırlatmaz), ama bu, Faz 7 (proxy/IP rotasyonu) devreye girene kadar yüksek hacimli mağaza sorgusu senaryolarında beklenmesi gereken kalıcı bir kapasite sınırı olarak not edildi (bkz. `.claude/PENDING_INPUTS.md`).

**Canlı doğrulama — üçüncü tur (derlenmiş üretim kodu, gerçek Chrome kanalı — bu makinede kurulu olduğu anlaşılınca yapıldı)**: `PlaywrightZaraFetcher`/`ZaraStockApiClient` sınıfları, RabbitMQ'yu atlayan küçük bir harness üzerinden (gerçek Redis + gerçek `Channel="chrome"` Playwright, aynı üretim kodu) doğrudan çalıştırıldı — aynı 10 ürün, 2'şer beden (online), ve ürün başına rotasyonla 4 mağazanın hepsini kapsayan birer mağaza sorgusu. Sonuç:
- **Online stok: 10/10 ürün doğru** — `PlaywrightPdpFetcher`'ın `window.zara.viewPayload`'dan okuduğu veriler, tarayıcı-tabanlı keşifle bulunanlarla birebir tutarlıydı (ör. "100 keten gömlek" ürünü bu turda her iki bedende de stoksuz çıktı — önceki turda XS `low_on_stock` idi, aradan geçen sürede gerçek stok değişmiş, bu bir hata değil).
- **Mağaza stoğu: 10/10 sorgu `Unknown`** — hepsi gerçek `403` ile başarısız oldu. Bu turda BULUNAN VE DÜZELTİLEN bir gözlemlenebilirlik hatası: `FetchStoreAvailabilityScript` eskiden `!res.ok` durumunda doğrudan `null` dönüyordu, health log'a yalnızca SAYFA NAVİGASYONUNUN durumu (200) yazılıyordu — asıl fetch'in gerçek 403'ü tamamen gizli kalıyordu (`success:false, httpStatusCode:200` gibi yanıltıcı kayıtlar üretiyordu). Script artık her zaman `{status, ok, body}` döner, health log artık gerçek durumu (`httpStatusCode:403`) doğru yazıyor.
- **En önemli bulgu — blok muhtemelen IP bazlı**: bu 10 mağaza sorgusu, İKİNCİ turdaki (tarayıcı-tabanlı, `claude-in-chrome`) testten TAMAMEN BAĞIMSIZ bir Playwright/Chrome sürecinde (farklı çerez kavanozu, farklı tarayıcı oturumu, ayrı bir process) çalıştırıldı — yine de aynı makinenin aynı ağından geldiği için TÜMÜ 403 aldı. Bu, ikinci turda "oturum/çerez bazlı" olarak nitelenen bloklamanın aslında (en azından kısmen) IP/ağ itibarına dayalı olduğunu ve TARAYICI OTURUMU YENİLENSE BİLE geçmediğini gösteriyor — yalnızca gerçek bir IP değişikliği (farklı ağ, ya da Faz 7'deki proxy/IP rotasyonu) bunu çözebilir.

Bu üçüncü tur, **derlenmiş `StockTracker.ZaraScraper` üretim kodunun** (RabbitMQ katmanı hariç, ama `PlaywrightZaraFetcher`+`ZaraStockApiClient` birebir aynı) gerçek Chrome ile gerçek Zara'ya karşı çalıştığını ve online akışın uçtan uca doğru sonuç verdiğini kanıtladı. Hâlâ eksik: RabbitMQ üzerinden gerçek `CheckStockCommand`→`StockResultEvent` akışının (`dotnet run --project StockTracker.ZaraScraper` ile tam servis, consumer dahil) doğrulanması — Bershka'nın 5 turluk sürecindeki son adım.

**DÜZELTME (dördüncü tur) — blok IP bazlı DEĞİL, otomasyon tespiti**: kullanıcının "tarayıcıdan sağlayabiliyorum, kod neden sağlayamıyor" itirazı üzerine yukarıdaki "muhtemelen IP bazlı" sonucu doğrudan test edilip ÇÜRÜTÜLDÜ:
1. Playwright'a kalıcı bir tarayıcı profili (`LaunchPersistentContextAsync`, diskte saklanan `user-data-dir`) + organik gezinme simülasyonu (anasayfa→kategori→ürün, aralarda gerçekçi bekleme) eklenerek tekrar denendi — YİNE `403`.
2. Kullanıcının GERÇEK tarayıcı profiliyle (`claude-in-chrome`) aynı anda, aynı endpoint'e, aynı productId/storeId ile atılan istek `200` başarılı oldu — Playwright TAM O SIRADA `403` alırken. Aynı makine, aynı IP, aynı an, farklı sonuç → **IP bazlı bir blok olamaz**.
3. Alternatif olarak Selenium+ChromeDriver (Playwright'ın CDP'sinden farklı bir otomasyon protokolü) denendi — O DA `403` aldı (hatta PDP navigasyonu da eksik veri döndü).

**Sonuç**: engelleme, Akamai'nin herhangi bir tarayıcı-otomasyon aracını (ChromeDriver'ın enjekte ettiği `$cdc_` değişkenleri, `navigator.webdriver`, headless'a özgü diğer sinyaller) tespit etmesinden kaynaklanıyor — IP/ağ itibarı değil, otomasyonun KENDİSİ tespit ediliyor. **Bu nedenle Faz 7'deki proxy/IP rotasyonu bu sorunu çözmeyecek** — önceki turlarda yazılan "IP değişikliği/proxy rotasyonu çözer" değerlendirmesi geçersiz. Daha derin çözümler (canvas fingerprint sahteciliği, CDP/WebDriver izi gizleme yamaları — ör. "undetected-chromedriver" tarzı) kasıtlı olarak denenmedi, çünkü bunlar bu bölümün altındaki "CAPTCHA çözme veya bot-tespitini aktif olarak atlatma gibi teknikler kapsam dışıdır" ilkesini aşıyor. **Mevcut durum bilinçli olarak kabul edildi**: online stok sorunsuz çalışıyor, mağaza sorgusu şu an için bilinen ve kalıcı bir kısıtlama (kod güvenli davranıyor — `Unknown` döner, çökmez). Detay: `.claude/PENDING_INPUTS.md`.

### Mango Scraper (Faz 6.1 — Tamamlandı, uçtan uca canlı doğrulandı)

Zara'dan sonra üçüncü marka — kullanıcı Zara'nın mağaza-sorgusu engelini araştırırken ("yarın devam edelim") paralel olarak öne alındı. Aynı canlı-keşif disiplimi uygulandı (shop.mango.com/tr/tr üzerinden gerçek network trafiği + `curl`), ve **Bershka/Zara'dan köklü şekilde farklı, çok daha basit bir mimariye** yol açtı:

- **En büyük fark: HİÇBİR bot koruması yok.** Ne PDP sayfası ne de mağaza stok API'si Akamai/benzeri bir sistemin arkasında — gerçekçi bir `User-Agent` + `Accept-Language` header'ıyla düz `curl` bile tam veriye erişiyor (canlı doğrulandı: varsayılan `curl` 403 alıyor ama bu yalnızca eksik header'lardan kaynaklanıyor). Sitede `api.eu.transmitsecurity.io` adında bir bot/fraud-tespit SDK'sı (Transmit Security — Akamai'den farklı bir sağlayıcı) tespit edildi, ama bu PDP/stok endpoint'lerini gerçekten bloklamıyor (yalnızca arka planda telemetri/risk puanı topluyor). **Sonuç: bu scraper'da Playwright/gerçek Chrome YOK** — hem online hem mağaza stoğu düz, `StockTracker.Shared.Scraping` ile dayanıklılık politikalı bir `HttpClient` üzerinden okunuyor. Bu, üretim kodunun derlenmiş haliyle gerçek Mango'ya karşı tam uçtan uca (RabbitMQ hariç, `MangoPdpFetcher`+`MangoStockApiClient` birebir) çalıştırılıp doğrulanan İLK marka (Bershka/Zara'da bu adım Playwright/Chrome bağımlılığı yüzünden bu ortamda hiç yapılamamıştı).
- **Ürün kodu formatı**: `8 haneli referans / 2 haneli renk kodu` (ör. `37013869/56`). `online-orchestrator.mango.com/v4/products?productId=X` API'sinin `"reference":"37013869"` alanından ve ürün URL yapısından (`.../37013869/56/00`) doğrulandı. **Medium confidence** tutuldu (Zara/Bershka'nın High'ının aksine) — temel 8 haneli referans kesin ama fiziksel üründeki TAM görünen format (ayraç dahil) doğrulanamadı. Bilinçli olarak `/` ayraçlı bileşik kod seçildi: yalnızca `^\d{8}$` olsaydı, Pull&Bear'ın (henüz doğrulanmamış) aynı deseniyle birebir çakışırdı.
- **Online stok**: Next.js App Router'ın React Server Components ("RSC") akışı — `window.__NEXT_DATA__` gibi klasik bir global YOK, `self.__next_f.push([N, "çift-escape'li JSON string"])` şeklinde `<script>` parçaları var. İçlerinden birinde `"colors":[{"id":"77",...,"sizes":[{"label":"XS",...,"available":true,"warehouses":[...]}]}]` — HER BEDEN için doğrudan bir `available` boolean. Zara'nın "in_stock"/"low_on_stock" gibi yorumlanması gereken string enum'larına gerek yok. `MangoPdpFetcher`, bu çift-escape'li yapıyı doğru ayrıştırmak için `System.Text.Json` ile ÖNCE dış diziyi (`[N,"..."]`) normal deserialize edip iç string'i (tek seviye escape) elde ediyor, SONRA o string içinde `"colors":[` için parantez dengesi (bracket-balance) ile alt diziyi bulup ayrıca ayrıştırıyor — elle string-replace ile unescape YAPILMIYOR (unicode/ters slash gibi diğer escape'leri bozma riski).
- **Mağaza stoğu — Zara'dan TAMAMEN FARKLI bir mekanizma**: `api.shop.mango.com/ncs/store-finder/v2/stores/stock?colorId=X&productId=X&countryIso=TR&languageIso=TR&channelId=shop&latitude=X&longitude=X` — belirli bir mağaza ID'siyle değil, bir **enlem/boylam etrafında "yakındaki mağazalar"** araması yapıyor. CANLI VERİYLE DOĞRULANAN kritik bulgu: sorgulanan mağazanın KENDİ koordinatları verilirse (mesafe ≈ 0) o mağaza güvenilir şekilde sonuç listesinde çıkıyor — bu yüzden `store_db`'deki her Mango mağazası için gerçek enlem/boylam saklanıyor. Yanıt "seyrek" (sparse) — Zara'da doğrulanan davranışın aynısı: bir mağaza dönen dizide hiç yer almıyorsa, o mağazanın var olmadığı değil, o ürünü/rengi TAŞIMADIĞI anlamına geliyor (canlı veriyle çapraz doğrulandı: Cevahir AVM — gerçek, var olan bir mağaza — bir üründe hiç çıkmadı, farklı bir üründe gerçek verisiyle çıktı). Mango, Zara/Bershka'nın aksine tam stok ADEDİ değil, yalnızca "bu bedenler bu mağazada mevcut" listesi (`sizes: string[]`) döndürüyor — miktar bilgisi yok.
- **Mimari sonucu — `CheckStockCommand` (V2) genişletildi**: Mango'nun enlem/boylam gereksinimi nedeniyle `StoreLatitude`/`StoreLongitude` (nullable `double`, varsayılan `null`) eklendi — yalnızca Mango dolduruyor, Bershka/Zara için her zaman `null`. `Store` entity'sine (`store_db`) de aynı nedenle nullable `Latitude`/`Longitude` eklendi. Bu değişiklik hem Search Orchestrator'ın anlık akışına (`SearchOrchestratorService`) hem Stock Poller'ın periyodik akışına (`StockPollerService`) yansıtıldı — ikisi de artık Store Reference'tan gelen koordinatları `CheckStockCommand`'a taşıyor.
- **Servis mimarisi** (`StockTracker.MangoScraper`, port 5011): Bershka/Zara ile aynı iskelet (`CheckStockCommandConsumer`, `stock.check.mango` kuyruğu, `/health/scraper-stats`+`/health/scraper-failures`) ama **iki ayrı `HttpClient`** (biri PDP için sabit `BaseAddress` olmadan — `productUrl` her zaman mutlak; diğeri `api.shop.mango.com` için sabit `BaseAddress`), ikisi de `ScraperEtiquetteHandler`+`HostRateLimitingHandler`+`AddScraperResilience` zincirinden geçiyor (bot tespiti olmasa bile nezaket/güvenilirlik için — Bershka'daki gerekçenin aynısı). Redis cache-aside yalnızca PDP verisi için (`mango:pdp-sizes:{productUrl}`, 15 dk TTL); mağaza sorgusu her zaman canlı.
- **Mağaza referans verisi**: `store_db`'ye Bershka/Zara'nın mevcut 4 iliyle eşleşen gerçek Mango mağazaları eklendi (`AddMangoStoresAndCoordinates` migration): Bağdat Caddesi/Kadıköy (id `10389`), Cevahir AVM/Şişli (id `10277`, Bershka/Zara ile AYNI AVM), CEPA AVM/Çankaya (id `10403` — Kentpark'ta Mango mağazası çıkmadı, en yakın gerçek eşleşme CEPA), Forum Bornova/İzmir (id `10711`, Bershka/Zara ile AYNI AVM).
- **Uçtan uca canlı doğrulama (derlenmiş üretim kodu)**: RabbitMQ katmanı hariç (`MangoPdpFetcher`+`MangoStockApiClient` birebir üretim kodu, gerçek Redis) 2 gerçek üründe, online stok 4/4 doğru, mağaza sorgusu 2/2 gerçek sonuç (biri `false`, biri `true` — ikisi de gerçek API'den, `403`/hata yok) — health log'da `PdpFetch`/`StoreFinder` ikisi de `200`, 150-800ms yanıt süresi (Zara'nın Playwright navigasyonlarının saniyeler süren yanıt sürelerinden çok daha hızlı, bot koruması olmadığı için).
- Unit testler `tests/StockTracker.MangoScraper.Tests` — 27 test: `MangoPdpFetcherTests` (RSC akışı ayrıştırma — çoklu renk/beden, ilgisiz push parçalarının atlanması, boş sonuç, HTTP hatası, `lastUnits`'in hem gerçek boolean hem `"$undefined"` string'i olarak gelmesi), `MangoStockApiClientTests` (online/mağaza stok mantığı, sparse yanıtta gerçek mağazanın dizide yoksa `false` — Zara'daki kritik bulgunun aynısı, case-insensitive eşleşme, malformed productCode, `isLastUnit` aktarımı), `MangoStockCheckServiceTests` (koordinat eksikse online kontrole düşme — Mango'ya özgü bir davranış), `CheckStockCommandConsumerTests`. Geliştirme sırasında gerçek bir üretim hatası bulundu ve düzeltildi: `MangoPdpFetcher` verimi camelCase (`JsonSerializerDefaults.Web`) ile serialize ediyordu ama `MangoStockApiClient`'ın deserialize çağrıları aynı seçenekleri kullanmıyordu — alanlar sessizce boş/varsayılan değere düşüyordu (unit testte yakalandı, canlı testten önce düzeltildi).

### Stok Miktarı / Son Ürün Bilgisi — Üç Scraper'a Ortak Ekleme (Faz 6.1)

Kullanıcı talebiyle eklendi: "stokta son ürün mü, mağazaya gidene kadar bitebilir mi bilgisini kullanıcıya göstermek istiyorum." Önce her markanın API'sinin bu veriyi gerçekten verip vermediği canlı olarak doğrulandı — üçü de veriyor, ama FARKLI şekillerde:

- **Bershka mağaza sorgusu**: gerçek sayısal miktar (`sizeStocks[].quantity`, zaten hesaplanıyordu ama dışarı taşınmıyordu).
- **Zara mağaza sorgusu**: gerçek sayısal miktar (`sizesAvailability[].stock`).
- **Mango**: HİÇBİR sorguda sayısal miktar yok, ama HEM online (`lastUnits`) HEM mağaza (`sizesIds[].isLastUnit`) sorgusunda API'nin KENDİ verdiği doğrudan bir "son ürün" boolean bayrağı var (canlı doğrulandı — gerçek bir mağaza/üründe `isLastUnit:true` gözlemlendi).
- **Bershka/Zara online sorgusu**: hiçbiri sayısal miktar ya da "son ürün" bayrağı vermiyor (yalnızca string enum) — bu durumda ikisi de her zaman `null`.

**Sözleşme değişikliği**: `Messages.V1.StockResultEvent`'e `Quantity` (`int?`) ve `IsLastUnit` (`bool?`) eklendi — `ScraperSource`'ta zaten uygulanan emsalle (varsayılan değerli, opsiyonel alan) aynı şekilde, AYRI bir V2 tipi oluşturulmadan. Bunun `CheckStockCommand`'dan farkı: burada MassTransit'in JSON tabanlı serileştirmesi ve tüm servislerin AYNI paylaşılan `StockTracker.Shared.Contracts` tipini kullanması sayesinde, ne Subscription'ın ne Notification'ın consumer/idempotency/durum-geçişi mantığının hiçbir kod değişikliğine ihtiyacı olmadı. Notification Service'te tek değişiklik, kullanıcının ayrıca istediği üzere, bu yeni alanların bildirim METNİNE yansıtılması oldu (bkz. aşağıda Notification Service bölümü) — Subscription hiç değişmedi.

**Uygulama deseni** (üç scraper'da da aynı): her `IXStockApiClient`'ın `CheckOnlineStockAsync`/`CheckStoreStockAsync` metodları artık `Task<bool?>` değil `Task<StockCheckResult?>` (yerel `record StockCheckResult(bool InStock, int? Quantity, bool? IsLastUnit)`) döndürüyor — `InStock` eski `bool?`'un yerini alıyor, `null` hâlâ "Unknown" anlamına geliyor. `IsLastUnit`, Bershka/Zara'da miktardan TÜRETİLİYOR (`Quantity == 1`) çünkü bu markaların API'lerinde ayrı bir bayrak yok; Mango'da ise API'nin KENDİ bayrağından DOĞRUDAN geliyor — bu ayrım kod yorumlarında açıkça belirtildi, ikisi karıştırılmamalı.

**Canlı doğrulama**: Mango için derlenmiş üretim kodu (`MangoStockApiClient`) gerçek `shop.mango.com`'a karşı çalıştırıldı — daha önceki keşifte `isLastUnit:true` gözlemlenen gerçek bir mağaza/ürün/beden kombinasyonu (METROPOL/Ataşehir, store id 14494) tekrar sorgulandı, `IsLastUnit=True` doğru şekilde geldi; aynı ürünün online kontrolünde `IsLastUnit=null` (o beden için "son ürün" sinyali yok) ve `Quantity=null` (Mango hiçbir zaman sayısal miktar vermiyor) doğru şekilde gözlemlendi. Bershka/Zara için ayrı bir canlı tur yapılmadı — değişiklik bu iki markada davranış değil yalnızca zaten hesaplanan bir değerin (Bershka: `totalQuantity`, Zara: `stock`) dışarı taşınması, API çağrı sırası/mantığı hiç değişmedi.

Test sayıları: Bershka 26 (+2 yeni: miktar/son-ürün aktarımı, online kontrolde ikisinin de null kalması), Zara 23 (+1 yeni: miktar/son-ürün aktarımı), Mango 27 (+3 yeni: `isLastUnit` aktarımı, online kontrolde ikisi ayrı testte).

### H&M Scraper (Faz 6.1 — Kod tamamlandı, canlı testte bilinen bir kısıtlamayla karşılaşıldı)

Mango'dan sonra dördüncü marka — www2.hm.com/tr_tr üzerinden aynı disiplinle (gerçek network trafiği + `curl`) keşfedildi.

- **Ürün kodu formatı**: `7 haneli productid / 3 haneli artid` (ör. `1351887/001`). H&M'in kendi mağaza stok endpoint'inin (`/tr_tr/sis/tr/{productid}/{artid}`) URL yapısından VE PDP'nin `articleCode` alanının (`"1351887001"` = productid+artid birleşik) bölünmesinden doğrulandı. Zara/Bershka'nın High'ının aksine **Medium** tutuldu — temel format API üzerinden kesin ama fiziksel ürün etiketindeki tam ayraçlı görünüm doğrulanamadı (Mango'yla aynı gerekçe).
- **Bot koruması**: Akamai — canlı doğrulandı, `curl` (gerçekçi UA dahil) hem PDP hem mağaza stok endpoint'inde Zara'yla **birebir aynı** "Access Denied" sayfasını (403) döndürüyor. Bu yüzden Mango'nun aksine Playwright (gerçek Chrome kanalı) gerekiyor.
- **Online stok**: Next.js'in KLASİK Pages Router `__NEXT_DATA__` global'i — Mango'nun App Router RSC akışından çok daha basit, doğrudan `JSON.parse` ile okunabiliyor. `productPageProps.ssrAvailability.availability` (stokta olan tam 13 haneli SKU'ların dizisi) ve `.fewPieceLeft` (bu dizinin alt kümesi — "az kaldı" uyarısı, canlı doğrulandı) üzerinden okunuyor. Beden adı↔kod eşlemesi `aemData.productArticleDetails.variations[articleCode].sizes[]` içinde.
- **Mağaza stoğu — Mango'nun modeliyle aynı, ama yanıt semantiği FARKLI**: `/tr_tr/sis/tr/{productId}/{artId}?latitude=...&longitude=...&radiusinmeters=...&maxnumberofstores=...&brand=000&channel=02` — mağaza ID'si değil enlem/boylam ile "yakındaki mağazalar" sorgusu (Mango'yla aynı, bu yüzden aynı `Store.Latitude/Longitude` alanları kullanıldı). CANLI VERİYLE DOĞRULANAN kritik fark: yanıt Zara/Mango gibi SEYREK (sparse) DEĞİL — yarıçap içindeki TÜM mağazalar (stoksuz olanlar dahil) açık bir `traffLightInd` (R=stokta yok, Y=birkaç tane kaldı, G=stokta) ile dönüyor; R ve G çok sayıda canlı örnekle doğrulandı, Y için UI'nin kendi renk lejantı (`✅ Stokta / ⚠️ Birkaç tane kaldı / ❌ Stokta yok`) doğrulandı ama bu turda canlı bir örneğine rastlanmadı. Bu yüzden `HmStockApiClient`, hedef mağaza yanıtta hiç yoksa Zara'nın "yok=OutOfStock" kuralının TERSİNE Unknown dönüyor (H&M'de "mağaza var ama listede yok" hiç gözlemlenmedi, bu bir sorgu sorununa işaret eder). Ayrıca `avaiQty` alanı CANLI VERİYLE doğrulanan bir bulguya göre GERÇEK bir sayı DEĞİL — yalnızca `0`, `1000`, `2000` ya da `3000` gözlemlendi, hiç ara değer yok. Bu yüzden bilinçli olarak `Quantity`'ye taşınmıyor (`"Stokta 1000 adet var"` kullanıcıyı yanıltır) — yalnızca `IsLastUnit` (`traffLightInd == "Y"`) taşınıyor.
- **Mağaza referans verisi**: `store_db`'ye diğer markaların 4 iliyle eşleşen gerçek H&M mağazaları eklendi (`AddHmStores` migration): Bağdat Caddesi/Kadıköy (`TR0030`), Özdilek Park AVM/Şişli (`TR0028` — Cevahir'de H&M çıkmadı, en yakın gerçek eşleşme), CEPA AVM/Çankaya (`TR0007`, Mango'nunkiyle birebir aynı mağaza), İzmir Optimum AVM/Bornova (`TR0075` — Forum Bornova'da H&M çıkmadı, ama Optimum AVM'nin kendisi gerçekten Bornova ilçesinde).
- **Servis mimarisi** (`StockTracker.HmScraper`, port 5012): Zara'ya en yakın iskelet — `PlaywrightHmFetcher` (gerçek Chrome kanalı, PDP navigasyonu + store endpoint'i için in-page `fetch`), `stock.check.hm` kuyruğu. Mango'nunkinden farklı olarak store endpoint'i `Content-Type: application/json` header'ı OLMADAN `415` döndürüyor (canlı doğrulandı) — script bunu açıkça ekliyor.

**⚠️ CANLI TESTTE BULUNAN KRİTİK KISITLAMA — Zara'daki bulgunun H&M'de DAHA CİDDİ bir tekrarı**: derlenmiş üretim kodu (`PlaywrightHmFetcher`, gerçek Chrome kanalı) gerçek H&M'e karşı çalıştırıldığında **PDP navigasyonunun kendisi bile `403` aldı** — Zara'da yalnızca mağaza sorgusu etkilenmişken, H&M'de bu ONLINE kontrolü de bloke ediyor (scraper'ın tamamı şu an gerçek Chrome/Playwright ile çalışmıyor). AYNI ANDA, kullanıcının GERÇEK (otomasyonsuz) tarayıcı oturumundan aynı sayfaya erişim sorunsuz çalıştı — Zara araştırmasında kesinleştirilen bulguyla birebir tutarlı: bu IP bazlı bir blok değil, otomasyonun (Playwright, gerçek Chrome kanalı kullanılsa bile) kendisinin tespit edilmesi. Zara için zaten yapılan kapsamlı araştırma (Selenium/ChromeDriver, Firefox, WebKit — hepsi denendi, hiçbiri işe yaramadı) burada TEKRARLANMADI çünkü aynı kök nedenin (Akamai'nin otomasyon-tespiti) geçerli olduğu değerlendirildi — bkz. `.claude/PENDING_INPUTS.md` ve hafızadaki "Zara mağaza sorgusu Akamai bloğu" notu (Oracle Cloud VM ön-test planı H&M için de geçerli). **Sonuç**: kod doğru yazıldı ve unit testlerle (21 test) doğrulandı, ama gerçek Chrome/Playwright ile uçtan uca canlı doğrulama şu an bu ortamdan yapılamıyor — Zara'nın mağaza-sorgusu kısıtlamasıyla aynı kategoride, ama H&M'de scraper'ın TAMAMINI etkiliyor.

Test sayıları: H&M 21 test (`HmStockApiClientTests`, `HmStockCheckServiceTests`, `CheckStockCommandConsumerTests`) — hepsi mock'lanmış `IHmPdpFetcher` ile, gerçek Playwright'a ihtiyaç duymadan yazılıp geçirildi.

### Massimo Dutti Scraper (Faz 6.1 — Tamamlandı, hibrit bir mimari)

H&M'den sonra beşinci marka — massimodutti.com/tr üzerinden aynı disiplinle (gerçek network trafiği + `curl`) keşfedildi. Zara ile aynı grup (Inditex) ama mimarisi diğer HİÇBİR scraper'la birebir aynı değil — **hibrit** bir durum: online stok Zara/H&M gibi Playwright gerektiriyor, mağaza stoğu ise Bershka gibi Akamai'siz düz bir HttpClient ile çağrılabiliyor.

- **Ürün kodu formatı**: `8 haneli temel referans / 3 haneli renk kodu` (ör. `06244810/251`). Ürün sayfasındaki `#mdfrontw-state` SSR state'inin `reference` alanından (`"06244810-I2026"`) ve `colors[].reference` alanından (`"C06244810251-I2026"`, renk kodu `colors[].id`) doğrulandı. H&M'in 7+3 deseniyle ÇAKIŞMIYOR (ilk grup hane sayısı farklı). Mango/H&M gibi **Medium** tutuldu — temel format API/SSR verisiyle kesin ama fiziksel ürün etiketindeki tam ayraçlı görünüm doğrulanamadı.
- **Bot koruması — İKİ FARKLI SONUÇ AYNI DOMAIN'DE**: canlı `curl` testiyle doğrulandı:
  - Ürün sayfası (SSR HTML) VE `itxrest/2/catalog/.../detail` API'si: Akamai korumalı. Ürün sayfası düz `curl`'de gerçek içerik yerine bir `bm-verify` sorgu parametresiyle 5 saniyelik bir JS-yönlendirme (challenge) sayfası döndürüyor; `catalog/.../detail` API'si ise doğrudan `403 Service Unavailable` alıyor.
  - Mağaza stok API'si (`api/storefront/1/stores/{storeId}/products/{catEntryId}/available-sizes`): AYNI domain'de olmasına rağmen **korumasız** — düz `curl` (gerçekçi UA ile) `200` + gerçek, sayısal stok verisi dönüyor.
  - Sonuç: online stok için Playwright (gerçek Chrome kanalı) ŞART, mağaza stoğu için ise Bershka'daki gibi düz, dayanıklılık politikalı bir `HttpClient` yeterli — tek bir scraper içinde iki farklı erişim yöntemi.
- **Online stok**: Angular tabanlı SSR state script'i `#mdfrontw-state` (Mango'nun çift-escape edilmiş RSC akışının aksine doğrudan geçerli JSON) — `ITX_GET_PRODUCT_DETAIL_KEY.colors[].sizes[]` dizisinde her bedende `isBuyable` (boolean) ve `backSoon` (string "0"/"1") alanları var. Sayısal bir miktar ya da ayrı bir "son ürün" bayrağı YOK — `Quantity`/`IsLastUnit` bu markada SADECE online kontrolde her zaman `null` (mağaza kontrolünde gerçek bir sayı var, aşağıya bkz.).
- **Mağaza stoğu — GERÇEK sayısal veriyle çalışıyor, Zara'nın "belirli mağaza ID'si" modeliyle aynı**: `api/storefront/1/stores/{storeId}/products/{catEntryId}/available-sizes?physicalStoreIds={id}&sizeIds={mastersSizeId}` — Mango/H&M'in aksine enlem/boylam GEREKMİYOR, doğrudan mağaza ID'si (Zara'daki gibi) ve ürünün `catEntryId`/`mastersSizeId` değerleri (ikisi de yalnızca PDP'den çözülebiliyor) yeterli. Yanıt Zara/Mango'daki gibi SEYREK (sparse) — sorgulanan mağaza dizide hiç yer almıyorsa (canlı veriyle doğrulandı, Cevahir/Şişli örneği), o bedenin o mağazada YOK demek, Unknown değil. API gerçek sayısal stok adedi (`stock`) veriyor — `Quantity`'ye doğrudan taşınıyor, `IsLastUnit` Zara'daki gibi `Quantity == 1`'den türetiliyor.
  - **⚠️ Düzeltilmiş bulgu**: bu scraper'ın ilk sürümünde mağaza sorgusu yanlışlıkla genel mağaza BULUCU API'sine (`itxrest/2/bam/store/{storeId}/physical-store` — yalnızca il/ilçe bazlı mağaza listesi döner, enlem/boylam gerektirir) dayandırılmış ve bu API'nin döndürdüğü `receiveStockQuery` bayrağının Türkiye'deki tüm mağazalarda `false` olması "mağaza bazlı stok sorgusu bu ülke için desteklenmiyor" şeklinde yanlış yorumlanmıştı. Kullanıcının kendi tarayıcısında gerçek bir stok sonucu ("BEDEN: 36" gibi) gördüğünü bildirmesi üzerine yeniden araştırıldı: `receiveStockQuery` bayrağının mağaza stok sorgusuyla hiçbir ilgisi yok (muhtemelen mağazalar arası transfer/lojistik gibi tamamen farklı bir iç işlevi belirtiyor) — asıl stok verisi yukarıdaki AYRI `available-sizes` endpoint'i üzerinden geliyor ve gerçek mağaza ID'leriyle test edildiğinde gerçek sayısal stok (`stock: 1` gibi) dönüyor. Kod buna göre düzeltildi.
- **Mağaza referans verisi**: `store_db`'ye diğer markaların 4 iliyle eşleşen gerçek Massimo Dutti mağazaları eklendi (`AddMassimoDuttiStores` migration): Şişli için Cevahir AVM (`4483`, Bershka/Zara/H&M'inkiyle birebir aynı mağaza), Çankaya için Kentpark AVM (`4009`, Bershka/Zara'nınkiyle birebir aynı mağaza), Kadıköy'de gerçek bir mağaza çıkmadığından Anadolu yakasındaki en yakın gerçek eşleşme Hilltown AVM/Maltepe (`12013`), Bornova'da da gerçek bir mağaza çıkmadığından İzmir'deki en yakın gerçek eşleşme Karşıyaka Rönesans AVM (`12840`). `Latitude`/`Longitude` alanları yalnızca bu mağazaları keşfetmek için kullanıldı (mağaza bulucu API'sine sorgu atmak için) — çalışma zamanında mağaza stok sorgusu bu koordinatlara İHTİYAÇ DUYMUYOR, yalnızca `BrandSpecificStoreId` yeterli.
- **Servis mimarisi** (`StockTracker.MassimoDuttiScraper`, port 5013): `PlaywrightMassimoDuttiFetcher` (yalnızca online stok için, gerçek Chrome kanalı, `#mdfrontw-state` okuyup renk `catEntryId`'si + beden `mastersSizeId`'si dahil düzleştirilmiş bir beden listesi döner) + `MassimoDuttiStockApiClient` (hem PDP fetcher'ı hem de Akamai'siz mağaza stok API'sine giden ayrı bir `HttpClient`'ı enjekte eder — Bershka'nın "PDP Playwright, stok API'si düz HttpClient" desenine en yakın scraper), `stock.check.massimodutti` kuyruğu. 21 unit test (`MassimoDuttiStockApiClientTests`, `MassimoDuttiStockCheckServiceTests`, `CheckStockCommandConsumerTests`) — hepsi mock'lanmış `IMassimoDuttiPdpFetcher` ve sahte (`FakeHttpMessageHandler`) mağaza stok API'siyle, gerçek Playwright'a ihtiyaç duymadan yazılıp geçirildi. Mağaza sorgusu tarafı ayrıca gerçek massimodutti.com'a karşı `curl` ile canlı doğrulandı (gerçek `stock` değerleriyle).

### Beymen Scraper (Faz 6.1 — Tamamlandı, Playwright hiç gerekmiyor)

Massimo Dutti'den sonra altıncı marka — beymen.com/tr üzerinde canlı keşfedildi, ama bu keşif BENZERSİZ bir zorlukla yapıldı: **bu ortamın ağı/IP'si Beymen'in WAF'ı tarafından SİTE GENELİNDE engelleniyor** (bkz. aşağıdaki "Ortam-özgü erişim engeli" notu) — bu yüzden keşfin büyük kısmı kullanıcının kendi tarayıcısından paylaştığı gerçek `curl` istekleri üzerinden yapıldı.

- **Bot koruması — Akamai değil, Incapsula (Imperva)**: canlı doğrulandı — Beymen'in ana web sitesi (SSR sayfaları, hatta `robots.txt` bile) bu ortamdan hem düz `curl` hem `claude-in-chrome` (kullanıcının gerçek tarayıcı oturumunu sürmesi gereken araç) ile 403 + Incapsula interstitial iframe'i alıyor — kullanıcı KENDİ cihazından siteye ve ürün stok bilgilerine sorunsuz eriştiğini doğruladı. Bu, bu ortama ÖZGÜ bir engelleme (muhtemelen datacenter IP itibarı) — ama kritik bir bulgu: **gerçek stok API'leri (aşağıya bkz.) sitenin geri kalanından TAMAMEN AYRI ve bu engelin dışında** — bu ortamdan bile çerezsiz düz `curl` ile 200 + gerçek veri dönüyor. Yani Playwright/gerçek tarayıcıya HİÇ gerek yok, Mango'daki gibi düz bir `HttpClient` yeterli.
- **Ürün kodu formatı**: diğer markalardan FARKLI olarak ayraçsız, düz 7 haneli sayısal bir ID (ör. `1661415`) — renk/varyant, `productCode`'un bir parçası değil, TAMAMEN AYRI bir ürün ID'si olarak modelleniyor (`otherColorList` alanı bunu gösteriyor). Ürün URL yapısından (`/tr/p_{slug}_{id}`) ve `sf-api/api/product/{id}/productsummary` API'sinden doğrulandı, birden fazla gerçek örnekle (1661415, 1884189, 2049912, ...) 7 hane teyit edildi. Medium confidence (tek marka örneğiyle genellendi).
- **Online stok**: `POST /sf-api/api/product/{productId}/productsummary` — gövdede sabit, oturuma özel olmayan bir `X-API-Key`/`X-Client-Id` çifti var (front-end bundle'ına gömülü, canlı trafikten alındı). Yanıt `result.sizes[]` dizisinde her beden için `inStock`, **GERÇEK sayısal stok adedi** (`stockQuantity`) ve mağaza sorgusu için gereken `variantBarcode` veriyor. Body'deki `storeId` alanı fiziksel mağaza DEĞİL — 1 ve 2 birebir aynı sonucu veriyor (muhtemelen web/mobil kanal ayrımı), diğer değerler (0, 3+) `204 No Content` dönüyor.
- **Mağaza stoğu — AYRI bir endpoint, sparse yanıt, gerçek mağaza listesi**: `GET /api/store/getstorestock/{variantBarcode}?sellerCode=beymen&isOnlineExclusive=false&languageCode=tr` — o barkodu (spesifik bedeni) taşıyan TÜM fiziksel mağazaların listesini (`Name`, `DistrictName`, `CityName`, `Coordinate`, `Variants[]`) döner. Yanıt Zara/Mango/Massimo Dutti gibi SEYREK — o bedeni taşımayan mağaza dizide hiç yer almıyor (canlı veriyle doğrulandı). Bu API'de sayısal bir mağaza ID'si YOK — mağazalar kendi `Name` alanlarıyla (ör. "Beymen Suadiye") tanımlanıyor, bu yüzden `BrandSpecificStoreId` bu isim. Mağaza bazında gerçek bir sayı YOK — yalnızca `IsAboutToRunOut` (boolean, "azalıyor/tükenmek üzere" eşik sinyali, TAM "son ürün" anlamına gelmiyor) var; `Quantity` mağaza kontrolünde her zaman `null`, `IsLastUnit` bu bayraktan (dürüstçe belgelenmiş bir yaklaşıklık olarak) taşınıyor.
- **Mağaza referans verisi**: `store_db`'ye diğer markaların 4 iliyle eşleşen gerçek Beymen mağazaları eklendi (`AddBeymenStores` migration): Kadıköy için Beymen Suadiye, Şişli için Beymen Nişantaşı (ikisi de birebir ilçe eşleşmesi), Çankaya için Beymen Panora (Kavaklıdere de bir seçenekti), Bornova'da gerçek bir mağaza çıkmadığından İzmir'deki en yakın gerçek eşleşme Beymen Hilltown İzmir (Karşıyaka — Massimo Dutti'nin Bornova için seçtiğiyle aynı mağaza/gerekçe). `Latitude`/`Longitude` API'nin kendi `Coordinate` alanından bilgi amaçlı dolduruldu, çalışma zamanında kullanılmıyor.
- **Servis mimarisi** (`StockTracker.BeymenScraper`, port 5014): Playwright YOK — `BeymenApiClient` tek bir `HttpClient` ile hem `productsummary` (online + barkod çözme) hem `getstorestock` (mağaza) çağrılarını yapıyor, `ProductUrl`'e hiç ihtiyaç yok (diğer tüm markalardan farklı — PDP navigasyonu gerekmiyor). `stock.check.beymen` kuyruğu. 18 unit test (`BeymenApiClientTests`, `BeymenStockCheckServiceTests`, `CheckStockCommandConsumerTests`) — hepsi sahte (`FakeHttpMessageHandler`) HTTP yanıtlarıyla yazılıp geçirildi; her iki gerçek endpoint de ayrıca kullanıcının paylaştığı gerçek `curl` istekleriyle (ve bu ortamdan çerezsiz tekrarıyla) canlı doğrulandı.

**⚠️ Ortam-özgü erişim engeli (site geneli, Incapsula)**: bu keşif sırasında, Zara/H&M'deki Akamai bulgusuna benzer ama FARKLI bir WAF ürünüyle (Incapsula) ve DAHA GENİŞ kapsamda (Zara'da sadece mağaza sorgusu, H&M'de PDP dahil ama site geneli değil; Beymen'de SİTENİN TAMAMI — ana sayfa, `robots.txt` dahil) bir engelleme bulundu. Kullanıcı kendi tarayıcısından sorunsuz erişebildiğini doğruladı — bu ortamın ağı/IP'si (veya `claude-in-chrome` uzantısının bu ortamdaki footprint'i) engelleniyor. Neyse ki gerçek stok API'leri (`sf-api`, `/api/store/...`) bu engelin TAMAMEN dışında olduğu için scraper'ın kendisi bundan etkilenmiyor — ama bu, aynı kök nedenin (ortam-özgü/otomasyon tespiti) farklı markalarda farklı şiddette karşımıza çıkabileceğinin somut bir örneği. Detay ve planlanan Oracle Cloud VM ön-testi için bkz. `.claude/PENDING_INPUTS.md` ve hafızadaki "Zara + H&M + Beymen erişim bloğu" notu.

### Pull&Bear Scraper (Faz 6.1 — Tamamlandı, Massimo Dutti ile AYNI platform)

Beymen'den sonra yedinci marka — pullandbear.com/tr üzerinde canlı keşfedildi. Zara ile aynı grup (Inditex) ve Massimo Dutti ile **birebir aynı** alt-yapıyı (aynı "MD Front" tarzı platform) paylaşıyor — bu yüzden keşif Massimo Dutti'nin izlediği yoldan büyük ölçüde tekrar edildi ve aynı hibrit mimari ortaya çıktı.

- **Bot koruması**: Akamai — canlı doğrulandı, ürün sayfası düz `curl`'de (gerçekçi UA'yla bile) gerçek içerik yerine bir `bm-verify` sorgu parametresiyle JS-yönlendirme (challenge) sayfası döndürüyor (Zara/Massimo Dutti ile birebir aynı format). Gerçek tarayıcı (`claude-in-chrome`) sorunsuz geçiyor.
- **Keşif sırasında karşılaşılan ek zorluklar** (kod dışı, yalnızca araştırma sürecine özgü): (1) site önce tam ekran bir `<gender-selector>` custom element'i (shadow DOM içinde) gösteriyor — kategori linklerine ulaşmak için önce "Kadın"/"Erkek" seçimi yapılması gerekiyor; (2) ürün linkleri `cS=`/`pelement=` sorgu parametreleri içerdiği için tarayıcı aracının bazı çıktı filtrelerinde (çerez/sorgu string'i görünümlü olduğu için) yanlışlıkla gizleniyor, ham `getAttribute('href')` ile okunması gerekti.
- **Ürün kodu formatı**: `8 haneli temel referans / 3 haneli renk kodu` (ör. `07460338/250`) — `<product-modular>` custom element'inin `__product.detail` verisinden (`reference: "07460338-I2026"`, `colors[].reference: "C07460338250-I2026"`) doğrulandı. Fiziksel etikette görünen `displayReference` alanı ilginç bir şekilde Zara tarzı `7460/338` (4+3) formatında — ama bu yalnızca GÖRÜNEN etiket, API/sorgu için kullanılan gerçek kod hâlâ 8+3.
  - **⚠️ BİLİNÇLİ, BELGELENEN ÇAKIŞMA**: bu 8+3 desen Massimo Dutti'ninkiyle BİREBİR AYNI (aynı platform paylaşıldığı için). Saf regex tabanlı `BrandCodeSignature` eşleşmesi bu iki markayı formattan ayırt edemez — BrandDetection Service'in zaten var olan "birden fazla aday → manuel çözüm" akışı bu durumda devreye girecek. Bilinçli bir kısıtlama, bkz. `.claude/PENDING_INPUTS.md`.
- **Online stok**: `<product-modular>.__product.detail.colors[].sizes[]` — Massimo Dutti'nin `#mdfrontw-state`'inin AKSİNE bir SSR script değil, hydration sonrası dolan bir custom element JS özelliği (`isBuyable`, `backSoon`, `mastersSizeId`, `catentryId`). Bu yüzden `PlaywrightPullBearFetcher`, Massimo Dutti'nin sabit 3sn beklemesinin AKSİNE, `__product` alanı dolana kadar (veya 8sn zaman aşımına kadar) polling yapan bir script kullanıyor.
- **Mağaza stoğu**: `api/storefront/1/stores/{storeId}/products/{catEntryId}/available-sizes?physicalStoreIds={id}&sizeIds={mastersSizeId}` — Massimo Dutti ile **birebir aynı API şekli** (aynı platform), AYNI domain'de olmasına rağmen Akamai KORUMASIZ (canlı doğrulandı: düz `curl` ile 200 + gerçek sayısal stok). Enlem/boylam GEREKMİYOR, doğrudan mağaza ID'si (Zara/Massimo Dutti'deki gibi) yeterli. Yanıt SEYREK (sparse) — sorgulanan mağaza dizide hiç yer almıyorsa o bedenin o mağazada YOK demek. Gerçek sayısal stok (`stock`) dönüyor — `Quantity`'ye taşınıyor, `IsLastUnit` `Quantity == 1`'den türetiliyor.
- **Mağaza referans verisi**: `store_db`'ye diğer markaların 4 iliyle eşleşen gerçek Pull&Bear mağazaları eklendi (`AddPullBearStores` migration): Kadıköy için City's Kozyatağı AVM (Bershka'nın seçtiği mahalleyle aynı gerekçe), Şişli için Cevahir AVM, Çankaya için Kentpark AVM (ikisi de diğer markalarla birebir aynı mağaza), Bornova için Forum Bornova AVM — bu markada Massimo Dutti/Beymen'in aksine GERÇEK bir Bornova eşleşmesi bulundu, en yakın mağazaya gitmeye gerek kalmadı.
- **Servis mimarisi** (`StockTracker.PullBearScraper`, port 5015): `PlaywrightPullBearFetcher` (yalnızca online stok için, gerçek Chrome kanalı, polling ile `__product` okuyup renk `catEntryId`'si + beden `mastersSizeId`'si dahil düzleştirilmiş bir beden listesi döner) + `PullBearStockApiClient` (Massimo Dutti'nin `MassimoDuttiStockApiClient`'ıyla neredeyse birebir aynı kod, farklı `StoreChannelId` ve farklı productCode ayrıştırma yorumu dışında), `stock.check.pullbear` kuyruğu. 20 unit test (`PullBearStockApiClientTests`, `PullBearStockCheckServiceTests`, `CheckStockCommandConsumerTests`) — hepsi mock'lanmış `IPullBearPdpFetcher` ve sahte (`FakeHttpMessageHandler`) mağaza stok API'siyle yazılıp geçirildi. Mağaza sorgusu tarafı ayrıca gerçek pullandbear.com'a karşı `curl` ile canlı doğrulandı (gerçek `stock` değerleriyle, 3 farklı mağazada 4-6 arası gerçek stok).

### Stradivarius Scraper (Faz 6.1 — Tamamlandı, mağaza stoğu için kullanıcı işbirliğiyle bulunan gerçek REST API)

Pull&Bear'dan sonra sekizinci marka — stradivarius.com/tr üzerinde canlı keşfedildi. Inditex grubunun bir üyesi ama diğer hiçbir markayla platform paylaşmıyor; kendine özgü bir mimari.

- **Bot koruması**: Akamai — canlı doğrulandı, aynı `bm-verify` deseni. Gerçek Chrome kanalı (`Channel = "chrome"`) şart.
- **Ürün kodu formatı**: ayraçsız, düz 8 haneli sayısal kod — ürün URL'sinin sonunda (`/tr/{slug}-l06383188` → `06383188`). Fiziksel etikette görünen REF (`6383/188/450` — base/renk/ekstra) ile ilişkisi: `"0" + base(4) + renk(3)` birleşimi. Massimo Dutti/Pull&Bear'ın `^\d{8}/\d{3}$` deseniyle ÇAKIŞMIYOR (ayraç yok) — Medium confidence (tek marka örneğiyle genellendi, Beymen'deki gerekçeyle aynı).
- **Ürün sayfası keşfinde kritik bir tuzak**: `c{id}.html` görünümündeki linkler PRODUKT değil, renk-filtreli KATEGORİ sayfaları (`__NEXT_DATA__.props.pageProps.pageName === "ItxCategoryPage"`) — gerçek ürün linki yalnızca kategori GRID'inin içindeki `<img>` sarmalayıcı `<a>` etiketlerinde (`/tr/{slug}-l{8haneli}`) bulunuyor.
- **Online stok — SSR HTML, hiçbir API/JS-state YOK**: kategori sayfaları klasik Next.js (`__NEXT_DATA__`) kullanırken ürün detay sayfası TAMAMEN AYRI, daha eski bir Redux tabanlı mikro-frontend (`ProductModel`, `Redux` globalleri) ve `__NEXT_DATA__` İÇERMİYOR. Beden/stok verisi doğrudan sunucu tarafında render edilmiş HTML'de: `button[data-testid="size-item"]` her biri kendi `data-sku`'sunu (`p[data-testid="size-name"][data-sku]`) taşıyor; `disabled` niteliği stok dışı durumu, buton metnindeki "Stok tükenmek üzere" ifadesi düşük stok uyarısını (yaklaşık "son ürün" sinyali) gösteriyor. Bu yüzden Playwright yalnızca Akamai'yi geçmek için gerekli — hydration/polling GEREKMİYOR (Massimo Dutti/Pull&Bear'ın aksine).
- **⚠️ Mağaza stoğu — mimari DÜZELTME (kullanıcı işbirliğiyle bulundu)**: ilk canlı keşifte "MAĞAZA STOK DURUMU" modalının kapalı (closed-mode) bir Shadow DOM içinde render edildiği ve hiçbir düz REST API'nin bulunamadığı sonucuna varılmış, geçici olarak koordinat tabanlı Playwright UI otomasyonu (sabit viewport üzerinde fare/klavye girişi + `Page.Accessibility.SnapshotAsync()`) uygulanmıştı. **Bu bulgu YANLIŞTI** — kullanıcının kendi tarayıcısının DevTools Network sekmesinden paylaştığı 2 gerçek `curl` isteği, modalın aslında iki KORUMASIZ REST API çağrısı yaptığını ortaya çıkardı (Beymen'deki collaborative-debugging emsaliyle aynı yöntem, çünkü bu istekler bizim otomasyon araçlarımızın kendi trafiğinde hiç görünmemişti):
  1. `GET itxrest/2/bam/store/54009571/physical-store?latitude=...&longitude=...&provinceIso=...` — enlem/boylam ile "yakındaki mağazalar" araması, Massimo Dutti/Pull&Bear/Zara'daki gibi GERÇEK sayısal mağaza ID'si döndürüyor (yalnızca mağaza KEŞFİ için kullanıldı).
  2. `POST api/storefront/1/stores/54009571/skus-availability-in-stores/actions/filter?language=tr-TR` — gövde `{"physicalStoreIds":[...],"skus":[...]}`, GERÇEK sayısal stok adedi (`stock`) döndüren asıl stok-kontrol API'si. Yanıt SEYREK (sparse, Zara/Mango/Massimo Dutti gibi): sorgulanan mağaza/sku anahtarı yanıtta hiç yer almıyorsa o bedenin o mağazada YOK demek.
  - **Kimlik doğrulama**: `Authorization: Bearer {access_token}` — bu, sitenin HER YENİ misafir (guest) oturumuna otomatik verdiği bir JWT (canlı doğrulandı: `user_type=guest` çerezi, ~24 saat geçerlilik) — ayrı bir login/kayıt akışı GEREKMİYOR. Token, `IStradivariusPdpFetcher`'ın aynı PDP ziyaretinde `document.cookie`'den okunup online beden verisiyle birlikte dönüyor.
  - Bu bulgu sonucu **koordinat tabanlı UI otomasyonu TAMAMEN KALDIRILDI** — mimari artık Massimo Dutti/Pull&Bear'la aynı hibrit desende: online stok Playwright (Akamai için), mağaza stoğu düz, dayanıklılık-politikalı bir `HttpClient` (Bearer token ile).
- **Mağaza referans verisi**: `store_db`'ye eklenen 4 Stradivarius mağazası (`AddStradivariusStores` + `UpdateStradivariusStoreIds` migration'ları) Pull&Bear'ınkiyle BİREBİR AYNI AVM'ler — Kadıköy=City's Kozyatağı (ID `16879`), Şişli=Cevahir (ID `2859`), Çankaya=Kentpark (ID `2968`), Bornova=Forum Bornova (ID `2868`) — `physical-store` API'sinden GERÇEK sayısal ID olarak doğrulandı. `BrandSpecificStoreId` artık Zara/Massimo Dutti/Pull&Bear'daki gibi doğrudan bu sayısal ID (ilk sürümdeki metin-tabanlı arama sorgusu YERİNE).
- **Servis mimarisi** (`StockTracker.StradivariusScraper`, port 5016): `PlaywrightStradivariusFetcher` yalnızca online stok İÇİN Chrome kanalı gerektiriyor (Akamai), aynı ziyarette guest-session `access_token`'ı da okuyup dönüyor. `StradivariusStockApiClient` bu token + per-beden SKU'ları Redis'te (15dk TTL, guest-token'ın ~24 saatlik geçerliliği içinde rahatça sığıyor) önbelleğe alıyor, mağaza stok API'sini düz `HttpClient` ile (Massimo Dutti/Pull&Bear'daki `ScraperEtiquetteHandler`/`HostRateLimitingHandler` zinciriyle) çağırıyor — mağaza API'si korumasız ve ucuz olduğu için hiç önbelleklenmiyor. `stock.check.stradivarius` kuyruğu. 20 unit test (`StradivariusStockApiClientTests`, `StradivariusStockCheckServiceTests`, `CheckStockCommandConsumerTests`) — `IStradivariusPdpFetcher` mock'lanıp sahte (`FakeHttpMessageHandler`) mağaza stok yanıtlarıyla yazılıp geçirildi; her iki gerçek endpoint de ayrıca kullanıcının paylaştığı gerçek `curl` istekleriyle VE kullanıcının tarayıcı oturumunun guest `access_token`'ı kullanılarak bizim tarafımızdan da canlı doğrulandı (10 farklı gerçek üründe, 4 hedef mağazanın tamamında gerçek sayısal stok adetleriyle).

## Ölçeklenme Riski — Çoklu Marka Scraping ve Bot Tespiti

Faz 6.1'de Zara ve Pull&Bear (ikisi de tamamlandı) gibi scraper'lar eklenince, hem marka sayısı hem de (Faz 3.2 Stock Poller devreye girince) tekrarlanan periyodik istek hacmi artacak. Zara'nın mağaza sorgusunda bulunan bloklama (bkz. yukarı Zara Scraper) bu riskin somut bir örneği — ama Bershka'daki hız/IP bazlı risklerden FARKLI bir kategoride: kesinleştirilen bulguya göre (dördüncü tur) bu blok IP değil, otomasyon aracının (Playwright/Selenium fark etmeksizin) kendisinin tespit edilmesinden kaynaklanıyor, bu yüzden proxy/IP rotasyonu (Faz 7) bunu çözmeyecek. Bershka Scraper'da doğrulandığı gibi (bkz. yukarı) hedef siteler en azından User-Agent bazlı temel bot tespiti yapıyor; istek hacmi arttıkça bu tespit muhtemelen sıkılaşacak (rate-limit, 429/403, IP bazlı engelleme) — bu genel risk Bershka için hâlâ geçerli. Bu, tek bir servisin problemi değil — her yeni marka scraper'ının tekrar tekrar çözmemesi gereken, paylaşılan bir altyapı sorunu. Faz 2.6 olarak plana eklendi (bkz. `.claude/ROADMAP.md`):

### Faz 2.6 — Tamamlanan önlemler (`StockTracker.Shared.Scraping/Http`)

Hepsi paylaşılan projeye taşındı/eklendi — Zara/Pull&Bear geldiğinde kopyala-yapıştır yerine doğrudan kullanılabilir:

- **`HostRateLimitingHandler`**: her hedef host için token-bucket ile dakika başına istek bütçesi (varsayılan 60/dk) — `ScraperEtiquetteHandler`'daki istekler-arası rastgele gecikme yalnızca art arda İKİ isteği yavaşlatıyordu, toplam bütçe tanımlamıyordu. Süreç içi `static` bir dictionary'de host→bucket eşlemesi tutuluyor (birden fazla eşzamanlı stok kontrolü de aynı bütçeyi paylaşır).
- **`ScraperEtiquetteHandler`** (taşındı, güçlendirildi): artık User-Agent'ı TEK BAŞINA değil, `BrowserProfile` (UA + tutarlı `Accept-Language` + varsa `sec-ch-ua`/`sec-ch-ua-platform`/`sec-ch-ua-mobile`) olarak birlikte rotasyonluyor. Yalnızca Chromium tabanlı profillerde `sec-ch-ua*` dolu — Firefox/Safari gerçek tarayıcılarda bu header'ları hiç göndermez, bu yüzden o profillerde bilinçli olarak boş bırakıldı (aksi halde motor/tarayıcı tutarsızlığı kendi başına bir parmak izi sinyali olurdu).
- **`ScraperResiliencePolicies.AddScraperResilience()`**: üç katmanlı Polly zinciri —
  1. `RetryWithRetryAfterAwareness`: 5xx/408/network hatalarına ek olarak **429'u da** kapsar (eski `AddTransientHttpErrorPolicy` 429'u hiç kapsamıyordu — `HandleTransientHttpError()` predicate'i yalnızca 5xx/408). Sunucu `Retry-After` header'ı verdiyse ona uyulur, vermediyse exponential backoff'a düşülür (`ComputeRetryDelay` — ayrı, saf bir fonksiyon olarak test edilebilir).
  2. `TransientErrorCircuitBreaker`: 5xx için "normal" devre kesici (5 hata → 30 sn, eskisiyle aynı).
  3. `BotDetectionCircuitBreaker`: **403'e özel, ayrı ve daha agresif** bir devre kesici (2 hata → 2 dk) — 403'ün "sunucu arızalı" değil "bizi bot olarak işaretledi" anlamına gelme ihtimali yüksek olduğu için, aynı hızda tekrar denemek durumu kötüleştirebilir.
- Gerçek `api.inditex.com`'a karşı doğrulandı: yeni handler zinciri (rate limiting + header profili + 3 katmanlı Polly) gerçek bir online + mağaza stok sorgusunu sorunsuz tamamladı, health-log'a (Faz 2.5) doğru şekilde işlendi.
- 15 yeni unit test (`tests/StockTracker.Shared.Scraping.Tests`): host bazlı bucket izolasyonu, bucket tükendiğinde gerçekten bekleme (cancellation ile kanıtlandı, gerçek 60 sn beklenmeden), `ComputeRetryDelay`'in Retry-After'ı doğru önceliklendirmesi, 429'un retry-yapılabilir olması (eski davranışa göre regresyon testi), 403 devre kesicisinin eşik sonrası gerçekten `BrokenCircuitException` fırlatması, Chromium/Firefox/Safari profillerinin `sec-ch-ua` tutarlılığı dahil.

### Faz 2.6 — kapsam dışı bırakılan madde

- **Proxy/IP rotasyonu**: kullanıcıyla değerlendirildi — gerçek bir IP rotasyonu ya ücretli bir proxy sağlayıcısı (Bright Data, Oxylabs vb.) ya da kendi çok-bölgeli sunucu altyapımızı gerektiriyor; ücretsiz/public proxy listeleri güvenlik riski taşıyor ve genelde zaten bloklanmış, Tor ise Akamai gibi sistemlerce datacenter IP'lerinden bile agresif tespit ediliyor. Kullanıcı kararıyla **Faz 7**'ye (en son faz, sağlayıcı kararı verilene kadar pasif) ertelendi — bkz. `.claude/ROADMAP.md`.
- **Headless browser tespiti (Playwright'a özgü, kalıcı risk)**: Bershka Scraper PDP'leri Playwright ile çekiyor (bkz. yukarı) — bu, Akamai'nin korumasını geçmenin tek yolu ama kalıcı bir risk taşıyor. **Somut olarak doğrulandı**: Playwright'ın bundled Chromium'u (varsayılan), UA/`navigator.webdriver` maskeleme gibi standart önlemlere rağmen Akamai'den anında "Access Denied" aldı — yalnızca gerçek Chrome kanalını (`Channel = "chrome"`) sürmek bu engeli aştı. Yani mevcut çözüm bile fiziksel olarak gerçek Chrome kurulu olan makinelere bağımlı (headless Chromium yeterli değil) — bu, hacim arttıkça (özellikle Faz 3.2 poller devreye girince) başarı oranının düşebileceğine dair somut bir erken sinyal. Redis cache-aside (productUrl başına 15 dk TTL) bunu *geciktirir* (Playwright'ı seyrekleştirir) ama ortadan kaldırmaz — bu risk yukarıdaki host bazlı rate limiting/circuit breaker önlemleriyle *azaltılıyor*, proxy/IP rotasyonu olmadan tamamen ortadan kalkmıyor.

## Billing — App Store / Play Store In-App Purchase

**Karar**: ödeme, ayrı bir sanal pos/ödeme sağlayıcısı (iyzico, Paddle vb.) üzerinden değil, mobil uygulamanın (Faz 5.4) App Store ve Play Store'daki yerleşik abonelik satın alma (in-app purchase) akışı üzerinden alınacak. Billing Service kart bilgisiyle hiçbir zaman karşılaşmaz — yalnızca (1) mobil client'ın tamamladığı satın almayı Apple/Google'ın **server-to-server** API'lerine karşı doğrular, (2) abonelik yaşam döngüsü event'lerini (yenileme, iptal, ödeme başarısız, refund) her iki store'un kendi webhook mekanizmasından dinler.

**Gerekçe**:
- PCI-DSS yükü tamamen Apple/Google'a devroluyor — ayrı bir ödeme sağlayıcı entegrasyonu, sözleşmesi veya webhook imza doğrulaması altyapısı (iyzico/Paddle'a özgü) kurmaya gerek kalmıyor.
- App Store/Play Store politikaları, dijital/abonelik ürünler için zaten kendi IAP sistemlerinin kullanılmasını **zorunlu tutuyor** — üçüncü taraf bir ödeme sağlayıcı kullanılsaydı uygulamanın store'dan reddedilme riski olurdu. Bu karar hem teknik hem operasyonel riski aynı anda azaltıyor.
- Fiyatlandırma store konsollarında (App Store Connect / Play Console) tanımlanır — `Plans` tablosunda `Price` alanı **tutulmaz**, yalnızca her store'daki ürün ID'si (`AppStoreProductId`/`PlayStoreProductId`) referans olarak saklanır.

**Akış (planlanan)**:
1. Mobil client, App Store/Play Store'un native satın alma akışını başlatır (kullanıcı Apple ID/Google hesabıyla öder — Billing Service bu adımda hiç devrede değil).
2. Satın alma tamamlanınca client, aldığı receipt/purchase token'ı `POST /verify-purchase`'a gönderir.
3. Billing Service, ilgili store'un server API'sine karşı doğrular (Apple: App Store Server API; Google: Play Developer API) ve `Subscriptions` kaydını oluşturur/günceller.
4. Abonelik sonrası tüm yaşam döngüsü event'leri (otomatik yenileme, iptal, ödeme başarısız, refund) client'tan bağımsız olarak **webhook** ile gelir: Apple → App Store Server Notifications V2 (JWS-signed payload, Apple public key'iyle doğrulanır), Google → Real-time Developer Notifications (Cloud Pub/Sub push subscription, OIDC token doğrulaması). Her ikisi de `PaymentEvents` tablosuna normalize edilerek yazılır, `(Provider, EventId)` unique constraint'iyle idempotent işlenir.

**Sıralama bağımlılığı**: gerçek bir satın almanın uçtan uca (gerçek cihazda, gerçek Apple ID/Google hesabıyla) doğrulanması mobil client gerektiriyor — Faz 5.4 bu fazdan **sonra** geliyor. Faz 4'te doğrulama/webhook mantığı Apple/Google'ın sağladığı sandbox/örnek payload'larla test edilecek; "gerçek satın alma" testi Faz 5.4 tamamlandıktan sonra mümkün olacak. Bu, projenin genelindeki "canlı altyapıya karşı doğrula" prensibinden bilinçli, dokümante edilmiş bir sapma.

### Plan Modeli (Faz 4.1 — Tamamlandı)

- **`Plans`**: `Id`, `Name`, `MaxTrackedProducts`, `CheckFrequencyMinutes`, `AppStoreProductId`/`PlayStoreProductId` (nullable, gerçek store ürünü oluşturulana kadar `null`). Seed data: `Free` (3 ürün, 60 dk kontrol sıklığı) ve `Premium` (50 ürün, 5 dk) — sabit `Guid`'lerle (`BillingDbContext.FreePlanId`/`PremiumPlanId`), Subscription Service'in Faz 3.2 poller önceliklendirme kademeleriyle (yüksek/orta/düşük) tutarlı büyüklük mertebeleri seçildi (ileride bu iki sistem birbirine bağlanacaksa — ör. plan'a göre poller sıklığı — sayılar zaten aynı ölçekte).
- **`UserPlans`**: kullanıcı başına tek satır (`UserId` unique) — güncel planı temsil eder, plan değişikliği (Faz 4.2/4.3) satırı günceller, yeni satır açmaz.
- **Otomatik Free plan atama — event-driven, senkron değil**: Identity Service ile Billing Service arasında doğrudan bir HTTP bağımlılığı **kurulmadı** (kayıt sırasında Billing'e senkron çağrı yapılsaydı, Billing geçici olarak erişilemez olduğunda kullanıcı kaydı da başarısız olurdu — kabul edilemez bir bağımlılık). Bunun yerine: `AuthService.RegisterAsync`, kullanıcı oluşturulduktan **sonra** yeni bir mesaj (`Shared.Contracts.Messages.V1.UserRegisteredEvent` — `UserId`, `Email`, `RegisteredAt`) fanout olarak publish eder (fire-and-forget, `IPublishEndpoint.Publish`, kayıt akışını bloklamaz/başarısız etmez). Bunun için Identity Service'e ilk kez `AddStockTrackerRabbitMq` eklendi (yalnızca publish için, kendi consumer'ı yok). Billing Service, kendi bağımsız `billing-user-registered-events` kuyruğuyla bu event'i tüketip `UserPlanService.AssignFreePlanAsync` ile Free plan atar — idempotent (kullanıcının zaten bir planı varsa dokunmaz), MassTransit'in at-least-once teslimatıyla event iki kez gelse bile çift atama olmaz.
- **`GET /plans`** (aktif planların listesi) ve **`GET /users/{userId}/plan`** (kullanıcının güncel planı, `Plan` detaylarıyla birlikte) — ikincisi Faz 4.3'teki `GET /billing/limits/{userId}` limit kontrolünün üzerine kurulacağı temel sorgu.
- Unit testler: `tests/StockTracker.Billing.Tests` (Free plan atama, idempotent tekrar atama — mevcut planın ezilmemesi, aktif/pasif plan filtreleme, kullanıcı planı sorgulama var/yok senaryoları) ve `tests/StockTracker.Identity.Tests`'e eklenen 2 test (`UserRegisteredEvent`'in başarılı kayıtta doğru `UserId`/`Email` ile publish edilmesi; email zaten alınmışsa hiç publish edilmemesi).
- Gerçek altyapıya karşı uçtan uca doğrulandı: gerçek Docker Postgres/RabbitMQ + çalışan Identity/Billing servisleriyle — gerçek bir `POST /auth/register` çağrısı sonrası `UserRegisteredEvent` gerçek RabbitMQ üzerinden Billing'e ulaştı, `GET /users/{userId}/plan` doğru Free plan detaylarını (`MaxTrackedProducts=3`, `CheckFrequencyMinutes=60`) döndürdü — sonrasında test verisi temizlendi.

### Store IAP Doğrulama + Webhook (Faz 4.2 — Tamamlandı, gerçek Apple/Google hesabı hariç)

**Yeni şema**: `UserSubscriptions` (kullanıcı başına tek satır, `UserId` unique — `Platform`, `StoreTransactionId` (Apple `originalTransactionId`, ömür boyu sabit) / `PurchaseToken` (Google, yenilemede değişebilir), `Status` (`Active`/`GracePeriod`/`Cancelled`/`Expired`/`Refunded`/`Unknown`), `CurrentPeriodEnd`) ve `PaymentEvents` (`(Provider, EventId)` unique — idempotency).

**`POST /verify-purchase`** — mobil client'ın tamamladığı satın almayı doğrular, doğrulanırsa kullanıcıyı **hemen** Premium'a yükseltir (webhook'u beklemeden — satın alma anında erişim açılmalı, kullanıcı deneyimi beklentisi). Plan seçimi şu an her zaman Premium: `Plans.AppStoreProductId`/`PlayStoreProductId` gerçek store ürünleri oluşturulana kadar `null` olduğundan productId→plan eşlemesi henüz güvenilir değil — MVP'nin iki-planlı (Free/Premium) yapısıyla tutarlı, gerçek store ürünleri tanımlanınca genişletilebilir.

**Apple entegrasyonu**:
- `AppleAppStoreServerClient` — App Store Server API'ye (`GET /inApps/v1/transactions/{id}`, Sandbox/Production ortamı `APPLE_STORE_ENVIRONMENT` ile seçilir) kimlik doğrulaması, Apple'a özel bir JWT (ES256, `.p8` anahtarla imzalanmış, `kid`=KeyId, `iss`=IssuerId, `aud`="appstoreconnect-v1", `bid`=BundleId, 5 dk ömürlü — Apple'ın max önerdiği 60 dk yerine bilinçli olarak kısa tutuldu) ile yapılır.
- `AppleJwsVerifier` — hem App Store Server API yanıtındaki `signedTransactionInfo`'yu hem de webhook'un dış+iç zarfını doğrulamak için kullanılan ortak bileşen. Apple'ın JWS'i standart bir OIDC token gibi önceden bilinen bir signing key ile değil, **mesajın kendi header'ındaki `x5c` sertifika zinciriyle** doğrulanır.
  - ⚠️ **Bilinçli, dokümante edilmiş kapsam sınırlaması**: yalnızca imzanın `x5c[0]` (lider sertifika) ile eşleştiği doğrulanıyor — yani mesaj değiştirilmemiş VE o sertifikanın özel anahtarıyla imzalanmış. AMA o sertifikanın gerçekten Apple'a ait olup olmadığı (zincirin Apple'ın gerçek Root CA'sına kadar güvenilir olduğu) **doğrulanmıyor**. Prodüksiyon öncesi zorunlu sertleştirme: Apple'ın resmi Root CA sertifikasını güvenilir depoya ekleyip `X509Chain` ile doğrulamak. Bu adım bilinçli olarak dışarıda bırakıldı çünkü gerçek bir Apple ortamına erişim olmadan (bu oturumda yok) uydurma bir sertifika baytı gömmek güvenlik açısından riskli olurdu — bkz. `.claude/SECURITY.md`.
- `AppleWebhookProcessor` — App Store Server Notifications V2'nin `notificationType`/`subtype` alanlarını `SubscriptionStatus`'a eşler (`SUBSCRIBED`/`DID_RENEW`→`Active`, `DID_FAIL_TO_RENEW`+`GRACE_PERIOD`→`GracePeriod`, `EXPIRED`→`Expired`, `REFUND`/`REVOKE`→`Refunded`; ilgisiz tipler — ör. `DID_CHANGE_RENEWAL_STATUS` — durum değiştirmeden atlanır).

**Google entegrasyonu**:
- `GooglePlayDeveloperClient` — Play Developer API'ye kimlik doğrulaması, service account'un JWT-bearer OAuth2 akışıyla (RS256, service account JSON'daki `private_key` ile imzalanır, `token_uri`'ye değiştirilir) elde edilen bir `access_token` üzerinden.
- `GoogleOidcTokenValidator` — Cloud Pub/Sub push aboneliklerinin her istekte gönderdiği OIDC bearer token'ı doğrular. **Apple'ın aksine burada sabit/gömülü bir sertifikaya gerek yok**: Google'ın imzalama anahtarları (`https://www.googleapis.com/oauth2/v3/certs`) her doğrulamada **canlı olarak çekiliyor** — gerçek, resmi bir Google endpoint'ine karşı doğrulama yapmak hem mümkün hem güvenli (Apple'ın x5c'sinden farklı olarak burada "hangi sertifikaya güveneceğiz" sorunu yok, Google'ın kendi JWKS'i otorite).
- `GoogleWebhookProcessor` — Real-time Developer Notifications'ın resmi `notificationType` kodlarını (`1`/`2`/`4`/`7`→`Active`, `5`/`6`→`GracePeriod`, `3`/`10`→`Cancelled`, `12`→`Refunded`, `13`→`Expired`) eşler; RTDN mesajının kendisi son kullanım tarihini taşımadığından, işleme sırasında Play Developer API'sine best-effort bir sorgu yapılır (yapılandırılmamışsa `CurrentPeriodEnd` `null` kalır, durum yine de güncellenir).

**Ortak çekirdek — `PaymentEventProcessor`**: hem Apple hem Google webhook'unun (ve dolaylı olarak `verify-purchase`'ın) vardığı ortak nokta. `(Provider, EventId)` ile idempotency kontrolü yapar, eşleşen `UserSubscription`'ı (Apple: `StoreTransactionId`, Google: `PurchaseToken` ile) bulup günceller, `Status`'a göre `UserPlans`'ı senkronize eder (`Active`/`GracePeriod` → Premium, diğerleri → Free). Eşleşen abonelik yoksa (webhook, ilgili `verify-purchase` çağrısından önce gelirse) event yine de denetim amacıyla kaydedilir ama plan değişikliği yapılmaz.

**Gerçek altyapıya karşı uçtan uca doğrulama**: gerçek bir Apple/Google hesabı olmadığından (kullanıcı kararı — bkz. altta), self-signed bir sertifikayla App Store Server Notifications V2 formatında gerçek bir JWS (dış zarf + iç `signedTransactionInfo` zarfı, ikisi de doğru şekilde imzalı) üretilip gerçek Docker Postgres'e karşı çalışan Billing Service'e gönderildi. Sonuç: imza doğrulandı, önceden DB'ye elle eklenmiş eşleşen `UserSubscription` `Expired`'dan `Active`'e güncellendi, doğru `CurrentPeriodEnd` hesaplandı, `UserPlans` otomatik `Free`'den `Premium`'a yükseltildi, `PaymentEvents`e doğru `(Provider, EventId, EventType)` kaydı düştü. Aynı payload'ın tekrar gönderilmesi `PaymentEvents` satır sayısını 1'de sabit tuttu (idempotency doğrulandı). Bozuk/imzasız bir Apple payload'ı `400`, `Authorization` header'sız bir Google webhook isteği `401` ile doğru şekilde reddedildi. `POST /verify-purchase`'ın Apple/Google yapılandırılmamışken `422` ile nazikçe başarısız olduğu (500 değil) doğrulandı — bu doğrulama sırasında **gerçek bir hata bulunup düzeltildi**: `AppleStoreSettings.IsConfigured`/`GooglePlaySettings.IsConfigured`, `.env`'deki doldurulmamış `REPLACE_WITH_ENV` placeholder'ını "yapılandırılmış" sayıyordu (yalnızca null/boş kontrolü vardı, Faz 3.3'te SendGrid/FCM için eklenen `REPLACE_WITH_ENV` kontrolü burada unutulmuştu) — `AppleAppStoreServerClient.BuildAuthToken()` geçersiz bir Base64 stringini çözmeye çalışıp `FormatException` ile 500 dönüyordu. Düzeltildi, regresyon testleri (`SettingsConfigurationTests`) eklendi. Sonrasında test verisi (`billing_db`) temizlendi.

**Kullanıcı kararı — gerçek Apple/Google hesabı bu fazda kurulmadı**: Faz 3.3'teki Firebase kararıyla aynı desen — HTTP/JWT entegrasyonları gerçek API'lere göre tam yazıldı, env var'lar (`APPLE_ISSUER_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY_BASE64`, `APPLE_BUNDLE_ID`, `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON_BASE64`, `GOOGLE_PLAY_PACKAGE_NAME`, `GOOGLE_PLAY_PUSH_AUDIENCE`) placeholder olarak `.env`'de duruyor, gerçek değerler girildiğinde kod değişikliği gerekmez.

Unit testler `tests/StockTracker.Billing.Tests` — bu fazda 34 yeni test: `AppleJwsVerifier` (geçerli imza, tahrif edilmiş payload, bozuk format, eksik `x5c`), `PaymentEventProcessor` (idempotency, eşleşen/eşleşmeyen abonelik, Free⇄Premium senkronizasyonu, platform bazlı eşleştirme), `PurchaseVerificationService` (Apple/Google başarı/başarısızlık yolları, tekrar çağrıda satırın güncellenmesi — yeni satır açılmaması), `AppleWebhookProcessor` (notificationType→durum eşlemesi, geçersiz imza reddi, ilgisiz tiplerin atlanması), `GoogleWebhookProcessor` (yetkisiz istek reddi, notificationType eşlemesi, bilinmeyen tiplerin atlanması, bozuk payload), `SettingsConfigurationTests` (yukarıdaki regresyonun testi).

> **Routing hatası — bulunup düzeltildi**: Bu fazda eklenen `verify-purchase`/`webhooks/*` endpoint'leri servisin kendi route tanımında yanlışlıkla `/billing/` önekiyle map edilmişti. Gateway zaten `/api/billing` prefix'ini soyduğundan (bkz. yukarı — Gateway Route Yapısı) servisin KENDİ route'ları önekSİZ olmalı, tıpkı Product'ın `/lookup/{code}`'u veya Subscription'ın `/watches`'ı gibi. Bu haliyle Gateway üzerinden gelen gerçek istekler 404 alırdı — Faz 4.3'te `GET /limits/{userId}` eklenirken fark edilip `/verify-purchase`, `/webhooks/apple`, `/webhooks/google` olarak düzeltildi; hem doğrudan servise hem `http://localhost:8000/api/billing/...` üzerinden gerçek Gateway'e karşı yeniden doğrulandı.

### Limit Kontrol Middleware'i (Faz 4.3 — Tamamlandı)

- **`GET /limits/{userId}`** (Billing Service) — `UserPlanService.GetUserLimitsAsync`, kullanıcının güncel `Plan`'ının limitlerini döner. Kullanıcının henüz bir `UserPlan` satırı yoksa (ör. `UserRegisteredEvent` henüz Billing'e ulaşmadı — Faz 4.1'deki asenkron atama tamamlanmadan önceki kısa pencere) **404 dönmez**, Free plan limitlerine düşer — bu endpoint'in tek tüketicisi olan Subscription Service'i gereksiz yere bloke etmemek için bilinçli bir tasarım.
- **`WatchService.CreateWatchAsync` — limit kontrolü, dedup'tan SONRA**: kullanıcı zaten aynı `WatchGroup`'u (aynı ürün+beden+mağaza) takip ediyorsa, limit kontrolüne hiç girilmez — dedup nedeniyle yeni bir `UserWatch` satırı açılmayacağından, "slot" tüketilmiyor. Limit kontrolü yalnızca gerçekten yeni bir `UserWatch` oluşturulacaksa devreye girer: `IBillingServiceClient.GetUserLimitsAsync(userId)` çağrılır, kullanıcının mevcut `UserWatches` sayısı `MaxTrackedProducts`'a karşı kontrol edilir; aşılmışsa `POST /watches` `403 Forbidden` + `{error: "WATCH_LIMIT_EXCEEDED", message: "'{Plan}' planınızın izin verdiği en fazla N ürün takip limitine ulaştınız..."}` döner.
- **Fail-open tasarım kararı**: `IBillingServiceClient.GetUserLimitsAsync` bir istisna fırlatırsa (Billing Service geçici olarak erişilemez), `CreateWatchAsync` limiti aşılmamış varsayıp takibe **izin verir**, yalnızca bir uyarı loglar. Gerekçe: bu bir ödeme-kritik akış değil (kart bilgisi/işlem yok, yalnızca bir sayaç kontrolü) — "Billing geçici kesintide olduğu için kullanıcı temel bir özelliği (ürün takibi) hiç kullanamıyor" kullanıcı deneyimi riski, "limit teknik olarak aşılmadan bir kullanıcı fazladan bir-iki ürün takip etti" iş riskinden daha ağır basıyor. Bu, projenin genelindeki graceful-degradation felsefesiyle tutarlı (bkz. Faz 3.3 SMTP/FCM, Faz 4.2 Apple/Google client'ları) — ama onlardan farklı olarak burada "özellik hiç çalışmıyor" değil "limit geçici olarak gevşiyor" riski var, bilinçli olarak kabul edildi.
- **Plan downgrade — mekanizma Faz 4.2'de zaten kuruldu**: webhook'tan gelen bir iptal/expire event'i `PaymentEventProcessor` aracılığıyla `UserPlans`'ı otomatik Free'ye düşürüyor (bkz. yukarı). Faz 4.3'ün eklediği kısım, bu düşüşün Subscription Service tarafında gerçekten *uygulandığının* doğrulanması — `GET /limits/{userId}` düşen limiti yansıtır yansıtmaz, bir sonraki `POST /watches` çağrısı yeni (daha düşük) limitle karşılaştırılır.
- **Gerçek altyapıya karşı uçtan uca doğrulama**: gerçek Docker Postgres + çalışan Billing/Subscription servisleriyle — Free planlı (limit 3, henüz `UserPlan` satırı olmayan bir kullanıcı, Free'ye düşme davranışı da böylece dolaylı doğrulandı) bir kullanıcı 3 ürünü sorunsuz takip etti, 4. denemede `403`/`WATCH_LIMIT_EXCEEDED` aldı. Kullanıcı elle Premium'a (limit 50) yükseltilip aynı istek tekrarlandığında `201 Created` ile başarılı oldu — plan yükseltmesinin limiti gerçekten gevşettiği doğrulandı. Son olarak Billing Service tamamen durdurulup yeni bir takip isteği gönderildi — `201 Created` ile başarılı oldu (fail-open, ProductServiceClient/StockPollerJob'ın zaten sahip olduğu "bağlantı hatasını yut, loglayıp devam et" desenine paralel bir davranış). Sonrasında test verisi (`billing_db`, `subscription_db`) temizlendi.
- Unit testler: `tests/StockTracker.Subscription.Tests`'e 3 yeni test (limit aşımında `Success=false` + doğru `ErrorCode`, aynı `WatchGroup`'u tekrar izlemenin dedup nedeniyle limit kontrolüne hiç girmemesi, `IBillingServiceClient` istisna fırlattığında fail-open); `tests/StockTracker.Billing.Tests`'e 2 yeni test (`GetUserLimitsAsync` — planı olan kullanıcı gerçek planın limitlerini alır, planı olmayan kullanıcı Free'ye düşer).

## Bilinen Mimari Kararlar ve Riskler

| Karar | Açıklama |
|---|---|
| Servisler arası iletişim gateway'den geçmez | Performans ve dayanıklılık için — iç HTTP direkt yapılır |
| Database-per-service | Cross-service foreign key yok; ID referansları ve event'lerle tutarlılık sağlanır |
| Scraping ToS riski | Hedef sitelerin ToS'una aykırılık riski bilinçli olarak kabul edilmiştir |
| PostgreSQL `CREATE DATABASE IF NOT EXISTS` yok | Init script'te her DB için ayrı `CREATE DATABASE` kullanıldı |
| YARP transform çakışması | `PathPattern` + `PathRemovePrefix` ayrı transform bloklarına bölündü |
| Mac → Docker init script permission sorunu | `git update-index --chmod=+x` ile çözülür |
| MassTransit sürüm kararı | v9+ ticari lisans gerektirdiği için son açık kaynak sürüm olan 8.5.5'e sabitlendi |
| FluentAssertions sürüm kararı | v8+ ticari lisans (Xceed) gerektirdiği için son Apache 2.0 sürüm olan 7.2.0'a sabitlendi (`tests/*.csproj`) |
| Ödeme: App Store/Play Store IAP, ayrı sanal pos yok | Kullanıcı kararı — PCI kapsamı store'lara devroluyor, store politikaları zaten kendi IAP'lerini zorunlu tutuyor (bkz. yukarı, Billing bölümü) |